# Reasonix（Android 端）与 Cloud 版 Agent 能力差距分析

> 创建日期：XXXX-XX-XX  
> 状态：待跟进  
> 关联会话：数据治理汇报 HTML 生成任务

---

## 一、差距总览

| 序号 | 差距项 | Cloud 版行为 | Android 版现状 | 影响程度 |
|------|--------|-------------|---------------|----------|
| 1 | 技能市场/动态安装 | 可搜索 skill 市场，自动下载安装并挂载 | 无此能力，工具集固定 | 🔴 高 |
| 2 | 工具集可扩展性 | 支持运行时注入新工具 | 工具由引擎启动时注入，不可变 | 🔴 高 |
| 3 | 技能发现与推荐 | 收到任务后主动判断是否需要 skill，自动搜索匹配 | 直接拆解为原子操作，不尝试寻找专用 skill | 🟡 中 |
| 4 | 第三方 skill 生态对接 | 可连接 GitHub、npm 等渠道获取 skill | 无对接能力 | 🟡 中 |
| 5 | 计划模式（Plan Mode） | 规划→审批→执行两阶段流程 | 仅禁用部分工具，无审批网关 | 🔴 高 |
| 6 | 执行过程可视化 | 过程树状展示、结果可回溯 | 过程树状、完成后切换平铺，执行轨迹结构丢失 | 🟡 中 |
| 7 | 成本统计 | 按模型分组、口径统一、命中率可视化 | 混算模型、两页口径不一致、无模型维度视图 | 🔴 高 |
| 8 | 停止按钮失效 | 点击即中断执行、有明确反馈 | 点击停止无反应或有明显滞后，疑似未打通中断链路 | 🔴 高 |
| 9 | 执行状态动效与布局 | 执行中动态动画、状态置底可见 | 静态“执行中”无动效、位置在顶部与回复割裂 | 🟡 中 |
| 10 | 斜杠命令入口 | 点击弹命令列表、选中填入输入框并执行 | 斜杠点了没反应、无命令列表、与输入框无联动 | 🟡 中 |
| 11 | 任务页签展示不稳定 | 所有执行命令以树状展示、顶层为项目 | 任务时有时无、展示规则不透明、顶层层级不明 | 🔴 高 |
| 12 | 页签标题缺失 | 左上角显示当前页签标题（加粗），内容从第二行排 | 四个页签顶部无对应标题、信息从顶开始无层级 | 🟡 中 |
| 13 | 删除会话不清理缓存 | 删除会话时关联缓存/备份文件一并删除 | 删除后 .reasonix-backup 缓存残留，重开 APP 重新加载已删会话（token 影响待确认） | 🔴 高 |
| 14 | 目标模式/锁图标意义不明 | 功能有明确用途与新手引导 | 锁图标、目标模式红圈用途不明，弹窗无帮助，首次使用不知何用 | 🟡 中 |

---

## 二、详细分析

### 2.1 技能动态安装能力缺失

**场景复现**：用户要求调用 `front design skill` 设计麦肯锡级别 HTML 汇报。

**Cloud 版预期行为**：
1. 收到指令后，识别 "front design skill" 为外部技能
2. 调用 `search_skill` 或类似工具在技能市场中搜索
3. 找到匹配的 skill → 自动下载安装
4. 挂载后调用该 skill 完成任务

**Android 版实际行为**：
1. 检查自身工具列表 → 未找到该 skill
2. 无工具可执行搜索/安装操作
3. 退化为用现有工具（read_file → 分析 → write_file）手动完成
4. 结果可用但缺少 skill 特有的模板、最佳实践或风格约束

**根因**：
- 引擎启动时注入的工具集是封闭的
- 缺少技能发现（Skill Discovery）和动态加载（Dynamic Loading）机制
- 没有类似 npm/pip/skill-market 的后端支撑

### 2.2 工具集不可扩展

当前工具列表是静态编译/配置的，运行时无法新增或替换工具。这意味着：

- 无法按任务类型加载专用 skill（如 "PPT 生成 skill"、"SQL 优化 skill"、"前端设计 skill"）
- 所有任务都用同一套通用工具处理，缺乏领域特化
- 用户无法为常用场景安装/分享 skill

### 2.3 任务规划缺少"技能匹配"环节

Cloud Agent 的任务规划流程通常包含：

```
收到任务 → 拆解子任务 → 对每个子任务判断"是否有专用 skill？"
  ├─ 有 → 搜索 → 安装 → 调用
  └─ 无 → 用通用工具执行
```

当前 Android 版的流程是：

```
收到任务 → 拆解子任务 → 直接用通用工具执行
```

跳过了"技能匹配"这一环节，导致即便存在合适的 skill 也不会被利用。

### 2.4 计划模式（Plan Mode）机制不完整

**用户预期（对标 Claude Plan Mode）**：
1. Agent 先"三思"——分析任务、拆解方案、给出执行计划
2. 停下来等用户**审阅并批准**计划
3. 用户确认后 Agent 才开始执行（写入/修改）

**Android 版实际行为**：
1. 打开"计划模式"后，仅仅在系统提示词中**禁用部分写入工具**（write_file/edit_file/multi_edit/move_file/delete_range 等）
2. 没有"输出方案 → 暂停 → 等待用户批准 → 再执行"的两阶段交互流程
3. 没有明确的"提交计划供审批"的信号（如 Claude 的 Plan 按钮/卡片）
4. 对可用工具的感知也不完整：Agent 在只读模式下尝试写入时会直接被工具层拒绝，而非优雅地引导用户进入批准流程

**根因**：
- 计划模式实现为"工具白名单/黑名单过滤"，而非"状态机 + 审批流程"
- 缺少用户决策网关（plan 提交 → 用户确认 → 解锁执行）
- Agent 自身也没有在规划结束时主动暂停、等待确认的机制（只能靠系统提示词约束）

**改进建议**：
- 引入"规划 → 审批 → 执行"两阶段状态机：规划阶段只读，产出方案后置为"待审批"，用户批准后自动放开写入
- 在 UI 上提供"提交计划 / 批准执行 / 驳回修改"按钮，与 Claude Plan Mode 对齐
- Agent 在规划完成时应主动汇报方案并等待确认，而不是继续尝试执行

### 2.5 执行过程可视化不一致（树状 vs 平铺）

**用户观察到的现象**：
1. 用户发送命令后，执行过程以"树状结构"呈现（类似土豆/Claude Code 的任务树）：每个子任务/工具调用有层级、有状态（进行中/完成），用户能清楚看到"现在正在执行什么、接下来要做什么"
2. 执行完毕后，界面切换为"平铺"方式展示所有内容：树状层级消失，所有结果/日志平铺罗列，无法再看到执行时的步骤结构、先后顺序与嵌套关系

**用户预期**：
- 执行过程与执行结果采用一致的可视化形态（最好是树状/时间线，可回溯）
- 完成后仍能保留执行轨迹的结构化视图：哪些步骤成功、哪些失败、耗时多少、产物与步骤的对应关系

**Android 版实际行为**：
- 过程态与结果态展示割裂：执行中能看到树状结构，完成后只剩平铺文本，用户无法复盘"这个结果是怎么一步步得到的"
- 长任务（多步骤）完成后，平铺内容难以定位关键节点，调试与追溯成本高

**根因**：
- 执行视图与结果视图可能由两套不同的渲染逻辑/UI 组件控制
- 缺少统一的"执行记录"数据模型（步骤树 + 状态 + 产物引用），过程信息与结果信息未持久化关联

**改进建议**：
- 统一执行轨迹模型：所有工具调用以"树/时间线"持久化，过程与结果共用同一视图
- 完成后保留"步骤树 + 结果平铺"双视图切换，或默认展示步骤树、点击节点展开详情
- 支持折叠/展开已完成步骤，突出当前步骤与失败节点（红色标记），便于快速定位

### 2.6 成本统计口径不一致（会话详情页 vs 成本分析页）

**用户观察到的现象**：
- 会话详情（绘画）页面显示该会话成本**很低**，但成本分析页面显示同一会话成本**非常高**，同一会话出现两个对不上的数字

**实测数据（会话 s_ec968c6f，数据治理汇报任务，42 轮）**：
- 总成本 **¥1.0174**（pro ¥0.639 + flash ¥0.378）
- 输入 token **合计 392.9 万**（命中 398.6 万 + 未命中 40.6 万），输出 token 5.8 万 → 输入:输出 ≈ 68:1
- 消息构成：用户 11 条/510 字符，助手 35 条/8,437 字符，**工具返回 36 条/175,936 字符（用户消息的 345 倍）**
- 上下文膨胀证据：hitTokens 自第 2 轮 6,528 递增至第 42 轮 123,136（累积的历史工具输出每轮全量携带）
- **模型混用时间线（关键发现）**：
  - 第 1~18 轮（23:32~23:38，生成 HTML 阶段）：**deepseek-v4-pro**，18 轮 **¥0.639**
  - 第 19~42 轮（23:42 至今）：deepseek-v4-flash，24 轮 ¥0.378
  - 精确计价见 2.6.1 三价模型（pro: hit ¥0.025 / miss ¥3.00 / 输出 ¥6.00；flash: hit ¥0.02 / miss ¥1.00 / 输出 ¥2.00）
  - 会话顶层 model 字段 = deepseek-v4-flash（当前模型），但 costs 历史含 pro

**成本高的三大主因（实测确认）**：
1. 工具调用返回（read_file/write_file/edit_file 输出）永久留在上下文，42 轮累积 392.9 万输入 token
2. 成本大头在输入-未命中（¥0.719，占 70.7%）——未命中单价远高于命中（50~120 倍）
3. 会话前 18 轮混用 pro 模型（3 倍单价），贡献 ¥0.639，占总成本 ¥1.0174 的 63%

**两页不一致原因（口径差异，实测支撑）**：
- 绘画页（会话详情）：仅统计可见对话/输出 token → 数字小
- 成本功能页：统计全部 42 轮 costs 累计（含全部输入 token 全口径）→ ¥1.0174
- 差距可达 13 倍，根源是用户不可见的"工具返回输入"每轮全量计费

---

### 2.6.1 定价模型与成本构成（实测反推，严谨计费公式）

**通用计费公式（每轮）**：
```
Cost = hitTokens × P_命中 + missTokens × P_未命中 + completionTokens × P_输出
```

**实测反推定价（最小二乘拟合，残差=0，即引擎真实价格）**：

| 模型 | P_命中(¥/M) | P_未命中(¥/M) | P_输出(¥/M) | 命中:未命中 |
|------|------------|--------------|------------|------------|
| deepseek-v4-pro | 0.025 | 3.00 | 6.00 | **1:120** |
| deepseek-v4-flash | 0.020 | 1.00 | 2.00 | **1:50** |

**关键结论（严谨统计口径）**：
1. **同一模型的三个计费项价格相差悬殊**：命中输入几乎免费（¥0.02~0.025/M），未命中输入贵 50~120 倍，输出是未命中输入的 2 倍、命中输入的 100~240 倍
2. **缓存命中率与成本贡献必须分开看**：本会话 pro 命中率 85.9%、flash 92.4%（看似很高），但成本大头仍是"未命中输入"（70.7%）——因为未命中单价远高于命中，**命中率高 ≠ 成本低，要看未命中的绝对 token 量**
3. **缓存降本价值可量化**：若无缓存，pro 输入成本 ¥0.49→¥3.33、flash ¥0.31→¥3.28，缓存累计节省约 **¥5.8**

**本会话成本构成（42 轮，总 ¥1.0174）**：

| 计费项 | 金额 | 占比 | 说明 |
|--------|------|------|------|
| 输入-未命中 | ¥0.719 | 70.7% | 单价最贵 + 量大（pro 15.6万 + flash 25万 token） |
| 输出 | ¥0.214 | 21.0% | token 少但单价高（¥2~6/M） |
| 输入-命中 | ¥0.085 | 8.3% | 命中单价极低，成本可忽略 |
| **合计** | **¥1.0174** | 100% | pro ¥0.639 + flash ¥0.378 |

**统计口径必须公示的规则**：
- 输入 token 分「命中 / 未命中」两档计费，价格不同（差 50~120 倍）
- 输出 token 单独计费（注意：输出单价高于未命中输入单价）
- 工具调用的参数与返回内容计入输入 token（实测 36 条 tool 消息 176KB 字符，是成本大头）
- 重试、流式中断的 token 计入与否需公示
- 同一会话多模型混用时，按模型分别计价再合计（实测本会话 pro+flash 混用）

---

### 2.6.2 成本统计改进设计（用户提出，产品需求）

**总原则**：成本统计必须**按模型维度分组**，且**所有页面口径统一、可解释、可下钻**。计算遵循通用计费公式：`Cost = hitTokens×P_命中 + missTokens×P_未命中 + completionTokens×P_输出`，每个模型使用各自的 P_命中/P_未命中/P_输出（见 2.6.1 实测价格）。

**1. 按模型分组统计成本**
- 同一会话可能混用多模型（实测 pro+flash），三价各不相同，混算无法解释成本构成
- 成本计算与展示一律按模型分组：Pro 成本 / Flash 成本 / 其他模型成本 → 再合计会话总成本
- 每个模型内部再按「输入-命中 / 输入-未命中 / 输出」三项分列（通用统计口径：token 三分类 × 单价）
- 分组维度贯穿：会话级 / 日期级 / 项目级均支持模型维度下钻

**2. 成本页签：模型分区 + 当日双树状图 + 汇总**
- 顶部汇总卡：总成本、Pro 成本、Flash 成本及各占比；每项成本可展开为「命中/未命中/输出」三行
- 「当日成本」渲染**两根树状图**：
  - 树①：当日 Pro 成本明细（日期 → 会话 → Pro 轮次，可展开到单轮 hit/miss/completion token 数 + 三项金额）
  - 树②：当日 Flash 成本明细（日期 → 会话 → Flash 轮次，同上）
- 每棵树附小计与占比，一眼看清"今天钱花在哪个模型、哪个会话"
- 树节点的金额 = 该轮按三价公式复算结果，与 costs 记录核对一致（实测拟合残差=0，可复算验证）

**3. 会话页签与成本页签口径必须统一（核心）**
- 两页显示同一会话的成本、token 数必须**同源同口径**（同一份 costs 数据 + 同一套三价公式）
- 明确并公示统计口径定义，至少覆盖：
  - 输入 token：分「命中/未命中」两档；是否含系统提示词 / 工具 schema / **工具调用参数与返回内容**
  - 输出 token：单档计价（注意输出单价 > 未命中输入单价）
  - 三档单价随模型不同而不同（公示各模型价格表）
  - 重试、流式中断是否计入
- **实测结论：工具调用返回内容必须计入输入 token**（36 条 tool 消息 176KB 字符是成本大头，不可忽略）
- 若提供"仅对话成本"等子口径视图，必须显式标注，避免误解

**4. 会话明细：折线图双线展示缓存命中率（含成本对照）**
- 点击会话明细展开折线图：
  - **Pro 线**：该会话 Pro 各轮缓存命中率 = hit / (hit + miss)
  - **Flash 线**：该会话 Flash 各轮缓存命中率 = hit / (hit + miss)
- X 轴：轮次（或时间）；Y 轴：命中率 0~100%
- **重要认知（基于实测）**：命中率是"省钱效率"指标而非"成本水平"指标——本会话 pro 命中率 85.9%、flash 92.4%，但成本大头仍是未命中输入（70.7%）。**命中率高的轮次便宜，但若某轮 miss 绝对量大（如第 15 轮 miss=76,004），即使命中率高成本也高**。因此折线图必须配套以下元素：
  - **叠加第二 Y 轴「每轮成本」线**，让命中率波动与成本尖峰直接对应
  - 标注模型切换点（实测第 19 轮 pro→flash）
  - 高 miss 轮次高亮标记（实测第 15 轮 miss=76,004 / ¥0.23、第 19 轮 miss=83,979 / ¥0.085）
  - 可切换显示「命中率 / 未命中 token 绝对量 / 每轮成本」三视图，避免单看命中率产生误判

**5. 缓存价值量化展示（让用户看懂"缓存省了多少钱"）**
- 每会话/每日展示：实际成本 vs 若无缓存成本（全按未命中计）→ 差值 = 缓存节省金额
- 实测示例：本会话缓存节省约 ¥5.8（pro ¥2.84 + flash ¥2.97）
- 此指标能直观驱动用户"复用上下文/长会话"，也解释"为什么命中率那么高成本还高"

**6. 其他建议**
- 会话成本排行（倒序），引导用户拆分长会话避免上下文无限膨胀
- 成本异常提示：单轮 miss 超阈值（如 5 万 token）或单会话成本超阈值时提示
- 成本页提供"口径说明 + 价格表"折叠区，公示全部计算规则（三价模型）

**状态：产品需求已明确（模型分组 + 三价口径统一 + 双树状图 + 命中率/成本双轴折线图 + 缓存节省量化），待引擎侧实现**

---

### 2.7 停止按钮无响应（疑似代码 bug）

**用户观察到的现象**：
- 执行命令时界面出现「停止」按钮，点击后**没有反应**
- 或感觉有滞后性：点击后任务仍在继续，过很久才停（或一直不停）

**用户预期**：
- 点击停止 → 立即中断当前执行（模型请求 / 工具调用 / 后台 shell 任务）
- 停止过程有明确反馈（按钮变灰 → “正在停止…” → “已停止”）

**可能原因（需排查代码）**：
1. **事件绑定问题**：停止按钮的 onClick / 监听器未正确绑定，或绑定在动态创建的节点上但未重新挂载
2. **前端未传递到后端**：点击只更新了 UI 状态（如按钮图标），但未向执行线程/HTTP 层发送取消信号（AbortController / cancel token）
3. **后端无可中断机制**：任务执行没有处理取消/中断信号——模型流式请求、工具调用、shell 命令均未实现中断（如 fetch abort / 进程 kill / 协程 cancel）
4. **流式响应中断不彻底**：只停止了 UI 渲染，底层请求仍继续跑完，导致“感觉停了但还在动”
5. **线程/进程阻塞**：停止操作在主线程执行被长任务阻塞，或 shell 子进程未纳入进程组管理（kill 不到）
6. **状态不同步**：任务已结束但 UI 仍显示“执行中”，或反之，停止按钮状态与实际执行状态脱节
7. **异步时序问题**：停止信号发出去但被后续轮次覆盖，无“已请求停止”状态锁存

**改进建议**：
- 点击停止立即有 UI 反馈（按钮变灰、文案变“正在停止…”），再异步执行真正的中断
- 打通中断链路：前端 click → AbortController → 后端任务句柄（fetch / shell / 工具执行）全部挂接 cancel
- 区分「已收到停止请求」与「已停止」两个状态，超时（如 5 秒）后强制终止并提示
- 后台任务（bash_output / shell）执行前登记 pid/进程组，停止时 kill 进程组，防僵尸
- 停止完成后回执“已停止”，并把已执行部分的结果保留展示

---

### 2.8 执行状态动效与布局优化（对标 Claude）

**用户观察到的现象**：
1. 「执行中」只是静态文字 + 3 个点，**没有任何动效**，用户无法感知“它真的在执行”
2. 布局割裂：先放一个 to do list，下面再回一段话（带句号、静止）；执行状态只挂在**上方的 to do list** 上，与回复内容脱节
3. 等待执行中的状态不在视线焦点，用户要抬头找，体验差

**用户预期（对标 Claude）**：
- “执行中”字样应**不停变动、闪烁、等待动画**（打字机光标、波浪点、旋转图标、进度条等），让用户明确感知“正在执行”
- 执行状态应**固定在最底部（贴近输入框）**，用户始终可见，不用滚动
- 布局统一协调：to do list 与执行状态、回复内容在同一视觉流中

**改进建议**：
- 执行中状态用 CSS 动画：打字机光标闪烁、点号波浪（. . . 依次亮起）、旋转 spinner、或当前动作文案轮播（“正在读取文件… / 正在写入… / 正在调用工具…”）
- 状态指示器**固定底部（sticky footer）**，贴近输入框，跟随最新动作实时更新
- to do list 中的当前项高亮 + 动效，完成项自动打勾收起；执行状态条与 to do list 联动
- 减少“先列表后静止段落”的割裂：把执行状态与回复区合并为统一的“执行流”，或让状态条始终在视口底部可见
- 长任务时状态条显示已用时间/当前步骤，增强“它还在干活”的信任感

**补充需求（用户追加，双位置动效）**：
- 动效需**双位置显示**，让用户抬眼/低头都能看到执行状态：
  1. **历史/to do list 上方**：保留列表动效（当前项高亮、状态变更）
  2. **输入框上方、最新一条消息下方**：生成一条**执行指示条**，显示「正在执行，请稍后…」并带动效（打字机光标 / 波浪点 / 旋转 spinner）
- 位置要求：紧贴**最新消息底部与输入框顶部之间**——用户视线聚焦输入框时一眼可见，无需滚动查找
- 行为要求：
  - 随消息流增长实时滚动跟随，指示条始终贴在最新一条消息下方
  - 动效文案可轮播当前动作（“正在读取文件… / 正在写入… / 正在调用工具…”），让用户知道在干什么
  - 执行结束自动消失或切换为完成态（如勾选 + “完成”），不残留
  - 与历史区动效样式统一，视觉一致

**补充需求（用户追加 #3，中间汇报文字下方等待提示）**：
- **场景**：Agent 思考/执行时，先建 to do list，下面紧跟一句话（中间汇报文字，如"好的，我先分析文档，然后生成 HTML"）。这句话**在等待 to do list 执行结果时才输出**，但它的**后面没有任何状态显示**——用户以为话说完了，仔细往上拉才发现 to do list 还在执行中
- **预期（对标 Claude）**：在这句话的**最下边**放一个动态提示：「**等待执行结果，请稍后…**」或「正在执行，请稍后…」，带动效（打字机光标 / 波浪点 / spinner）
- **设计要点**：
  - 位置：紧贴中间汇报文字末尾下方（该文字是"异步等待"状态，不是最终回复）
  - 动效：持续闪烁/变动，明确传达"还没说完，还在等结果"
  - 结束时切换为最终回复，提示条消失（或变完成态），避免用户误判"已说完了"
  - 与 to do list 执行状态、输入框旁指示条三处联动，但各有职责：
    - to do list 上方 = 任务进度
    - 中间文字下方 = "我在等结果，还没说完"
    - 输入框上方 = 底部常驻可见
- **移动端必要性（用户强调）**：PC 端 token 执行快、工具形态不同，等待感不明显；**手机端执行慢，更需要这类人性化交互**，否则用户体感差、容易误操作（如重复发送指令）

---

### 2.9 斜杠命令入口无作用（会话界面）

**用户观察到的现象**：
- 会话界面存在「斜杠（/）」按钮，点击**可以弹出命令面板**（命令列表能显示出来）
- 但**点击面板中的命令后没有任何反应**——命令既不回填到输入框，也不执行
- 对照 PC 版 Reasonix / Claude CLI：点击斜杠弹出命令列表，选中某条命令后，把该命令**填入输入框**（可编辑）并执行

**用户预期（对标 PC 版）**：
- 点击斜杠 → 弹出命令列表（常用命令：如切换模型、新建会话、上传文件、执行工具等）
- 支持输入筛选（输入关键词过滤命令）
- **选中命令 → 命令文本自动填入输入框（光标定位、可继续编辑）→ 用户回车执行**
- 再次点击斜杠或按 Esc 关闭命令列表

**可能原因（需排查代码）**：
1. **命令点击事件未绑定**：命令列表项渲染出来了，但每项的 onClick/touch 事件未实现或未绑定（列表是纯展示，无交互）
2. **回填逻辑缺失**：选中命令后没有执行「写入输入框」的动作——缺 focus + setValue + 光标定位的代码路径
3. **选中状态未处理**：命令项没有高亮/选中状态，点击只触发 UI 抖动但无后续动作
4. **事件冒泡被拦截**：命令面板的点击事件被父容器拦截（stopPropagation 误用 / 遮罩层挡住）
5. **数据源与动作分离**：命令面板只有静态数据渲染，没有把「命令标识 → 对应动作/文本」的映射挂到点击事件上
6. **触摸事件缺失**：移动端只绑定了 click，触摸点击未触发

**改进建议**：
- 为每条命令绑定完整交互：点击 → 高亮选中 → **命令文本回填输入框**（获得焦点、光标置末尾）→ 用户回车即执行
- 若命令本身是可即时执行的（如 /new、/clear），点击后直接执行并关闭面板
- 检查事件绑定与冒泡：确保命令面板点击不被遮罩层/父容器拦截，补 touchstart 事件
- 命令项补充选中态样式（高亮/勾选），点击有视觉反馈
- 回填后输入框可见（滚动到可视区域），用户能确认命令已就位

---

### 2.10 任务页签展示不稳定（任务时有时无）

**用户观察到的现象**：
- 「任务」页签里的任务**有时候有、有时候没有**，摸不准出现规律
- 预期：所有执行过的命令都应**以树状结构展示**，顶层是**项目**，下面挂该项目的各次任务/执行记录
- 现状：部分执行记录消失或未展示，且顶层层级（项目维度）不清晰，用户无法按“项目 → 任务 → 步骤”追溯

**用户预期（对标 Claude/通用任务树）**：
- 树状结构：**项目（顶层）→ 会话/任务 → 执行步骤/工具调用**，层级固定、可展开收起
- **所有执行过的命令都有记录**，不随刷新/时间/状态丢失
- 任务状态清晰：排队中 / 执行中 / 已完成 / 失败 / 已停止，每个状态可区分
- 点击任务可查看完整执行轨迹（步骤、耗时、工具调用、输出摘要）

**可能原因（需排查代码）**：
1. **任务生命周期不完整**：任务未落库/未持久化，只存在内存中，页面刷新或内存回收后消失
2. **只记录“成功/完成”任务**：排队中、失败、被停止、未结束的任务未写入任务列表
3. **过滤/分页逻辑问题**：任务列表按某种条件过滤（如只看当天/只看运行中/只看当前项目），导致时有时无
4. **异步未同步**：任务开始/结束事件未正确更新列表（如完成后从列表移除，或开始时未加入）
5. **多会话/多项目隔离错误**：任务列表与当前打开的项目/会话绑定，切换后看不到其他项目的任务
6. **数据源不一致**：任务页与历史记录各用一套数据，未统一
7. **并发/竞态**：任务并行执行时列表更新互相覆盖，部分任务被吞

**改进建议**：
- 任务持久化：每次执行命令即创建任务记录（含项目、会话、时间、状态），落库保存，刷新不丢
- 状态覆盖全：排队中/执行中/完成/失败/停止全部展示，可筛选但默认全显
- 树状结构固定：项目（顶层）→ 会话/任务 → 步骤/工具调用，支持展开收起与搜索
- 列表更新走统一事件流：任务状态变更广播到列表，避免竞态覆盖
- 任务页提供筛选（按项目/状态/日期），但默认展示全部，杜绝“摸不准”
- 任务记录附带：开始/结束时间、耗时、模型、token 用量、结果摘要，点击可下钻

**补充需求（用户追加，折叠/展开 + 顶层工程名）**：
- 后台任务树除持久化、全量展示外，还需支持**树状折叠与展开**交互（逐级展开/收起，记忆上次展开状态）
- **顶层明确为「工程名」**：工程（顶层）→ 会话/任务 → 执行步骤/工具调用，层级命名清晰、不可混淆
- 折叠态只显示工程名 + 任务计数/总耗时摘要；展开后逐层下钻到单条执行记录
- 展开动画平滑、长列表虚拟化渲染，避免大工程卡顿

---

### 2.11 页签标题缺失（四个页签顶部无标题）

**用户观察到的现象**：
- 四个页签（绘画/成本/任务等）顶部是四个圆圈（图标），但**没有对应的页签标题**
- 信息从最顶部直接开始排，缺少“当前在哪个页签”的明确标识，不符合通用设计规则

**用户预期（对标通用设计规则）**：
- 每个页签**左上角**显示当前页签标题（如绘画页签显示「**会话**」），**加粗**作为页头主标题
- 标题之下（第二行起）才排列功能内容：筛选、列表、图表等，形成“标题 → 内容”的层级
- 四个页签各自独立标题：会话页 = 「会话」，成本页 = 「成本」，任务页 = 「任务」，其他页签同理

**改进建议**：
- 页签容器顶部固定页头：左上角加粗标题（如“会话”），字号醒目，作为该页导航锚点
- 内容区从第二行开始排布，与标题保持间距；滚动时标题可吸顶（sticky）或随内容滚动
- 标题与页签图标联动：切换页签时标题同步切换，用户始终知道当前所在页
- 标题下方可附带页级说明/统计摘要（如会话数、今日成本、运行中任务数），增强信息层级

---

### 2.12 删除会话不清理关联缓存（最重要）

**用户观察到的现象**：
- 删除会话后，该会话相关的**缓存文件（.reasonix-backup 下的 JSON 备份）并没有随之删除**
- 实测：`.reasonix-backup` 目录下存有多个会话 JSON（最大达 1.4MB），即使会话在界面上被删除，文件仍残留

**用户担忧（已澄清修正）**：

✅ **确定危害（实测确认）**：
1. **重开 APP 再次加载已删除会话**：缓存文件残留，应用启动时会重新扫描发现并加载，用户会看到已删除的会话“复活”，体感差，像删除没生效
2. **占用存储**：残留文件占磁盘空间（实测最大 1.4MB，会话越多越大），无上限膨胀

❓ **待确认的担忧（用户理解：不会污染，需引擎侧验证）**：
- 用户理解：这些缓存**仅用于 App 二次打开时恢复/加载会话用**，上下文只在**具体会话内部**传递——即只有在那个会话下说话，才会把该会话的上下文（含工具调用等）传给模型计费
- 因此：**已删除会话的残留文件不会污染其他会话的上下文，也不会产生额外 token 计费**（除非用户重新进入该会话）
- ⚠️ 该理解需引擎侧确认：确认“发送请求时只携带当前会话上下文 + 系统提示词，不加载其他会话缓存”的设计成立，则 2.6 中“残留文件浪费 token”的担忧降级为“重开加载 + 占存储”两项确定危害

**可能原因（需排查代码）**：
1. **删除只删列表项，未删文件**：UI 层的“删除会话”只移除了内存/列表中的会话记录，未调用文件系统删除对应缓存文件
2. **缺少级联删除**：会话记录与缓存文件无关联删除逻辑（无事务/无回调），删除动作不向下传播
3. **文件引用遗漏**：部分缓存文件（如历史版本备份）未被纳入会话关联表，成为孤儿文件
4. **删除确认后未同步持久化**：删除操作未落盘（只更新了 UI 状态），重启后旧文件重新被发现加载

**改进建议**：
- **级联删除**：删除会话时，同步删除其在 .reasonix-backup 下的缓存文件（先确认路径归属再删，防止误删）
- **删除前确认 + 记录**：涉及文件删除属不可逆操作，弹确认框，并记录删除日志（会话 id、文件名、时间）
- **孤儿文件清理**：启动时扫描 .reasonix-backup，清理无对应会话记录的残留文件；提供“清理缓存”入口
- **删除即落盘**：删除操作持久化到存储层（数据库/元数据），重启后不再加载已删会话
- **成本联动**：删除后该会话不再计入任何 token/成本统计（成本页与会话页数据同源同步）
- 可选：删除前询问“是否同时删除缓存文件以释放空间”（避免用户想保留历史数据时误删）

---

### 2.13 目标模式 / 锁图标意义不明（UI 与逻辑需排查）

**用户观察到的现象**：
1. **绘画交互页面右上角有个「锁」图标**，不知道是干嘛的——若没用应删除，或明确其功能
2. **目标模式有个「红圈」标识**，非常难看，不符合通用设计标准（突兀、无含义、无美感）
3. **第一次点开目标模式，完全不知道是干什么用的**——没有任何说明/引导

**需排查项（用户提出）**：
1. **目标模式的逻辑是否正确**：触发条件、生效范围、与其他功能（如计划模式）的关系是否清晰、无逻辑漏洞
2. **交互弹窗的按钮是否合理**：按钮文案、数量、层级、是否会造成误操作
3. **里面是否有帮助**：是否提供功能介绍/说明文案/新手引导

**用户预期（对标通用设计标准）**：
- 每个图标/模式都有明确语义：或用文字标注、或用长按 tooltip、或首次使用弹引导
- 目标模式的视觉状态（如“目标中/未激活”）应有明确、美观的标识，不用突兀红圈
- 首次打开目标模式应出现引导：说明它能做什么、怎么用、如何退出
- 无用功能应移除，保留的功能应“一看就懂”

**改进建议**：
- **锁图标**：确认用途（是否锁定会话/防误删/隐私？）→ 有用则加文字提示与交互说明；无用则删除
- **目标模式视觉**：红圈替换为通用状态标识（如高亮胶囊/开关样式/状态徽章），符合设计规范
- **首次引导**：目标模式首次开启弹引导页（用途、操作、退出方式），或提供“?”帮助入口
- **逻辑自查**：梳理目标模式的完整状态机（未开启/开启/执行中/完成/取消），确认各状态切换正确、弹窗按钮（确认/取消/帮助）层级合理
- 交互弹窗：按钮遵循平台规范（主操作高亮、次要操作弱化、取消可 Esc），避免误触

---

## 三、改进建议

### 3.1 短期（可快速落地）

| 建议 | 说明 | 优先级 |
|------|------|--------|
| 内置常用 skill 到引擎 | 将高频 skill（如前端设计、数据分析、PPT 生成）预置到 Android 引擎中 | P0 |
| 支持用户手动注册 skill | 用户可通过配置文件或 UI 注入自定义 prompt/skill，Agent 在任务规划时参考 | P1 |
| 建立 skill → 回退映射 | 当缺少某个 skill 时，明确告知用户并询问是否用通用方式完成，而不是静默回退 | P2 |

### 3.2 中长期（架构升级）

| 建议 | 说明 |
|------|------|
| 技能市场后端 | 搭建 skill registry，支持搜索、版本管理、依赖解析 |
| 动态工具注入 | 运行时可通过 API 加载新的 tool schema + handler |
| 任务规划器升级 | 引入 skill matching 层，任务拆解后自动搜索最佳 skill |
| 离线 skill 缓存 | 常用 skill 本地缓存，减少运行时下载延迟 |

---

## 四、当前应对策略

在 skill 机制落地前，Agent 应：

1. **主动询问**：当用户提及未安装的 skill 时，明确告知当前无此能力，并询问是否可用通用方式完成
2. **记录缺口**：每次遇到 skill 缺失场景，追加到本文档，形成需求清单
3. **搜索补救**：对于公开可查的 skill（如 GitHub 仓库），尝试用 `web_search` + `web_fetch` 获取其 prompt/规范，手动模拟

---

## 五、缺口记录日志

| 日期 | 缺失 Skill | 场景 | 处理方式 |
|------|-----------|------|----------|
| XXXX-XX-XX | front design skill | 设计麦肯锡级别汇报 HTML | 手动编写，结果可用但未遵循 skill 模板 |
| XXXX-XX-XX | Plan Mode | 用户期望"三思后让用户执行"，实际只是禁用部分工具 | 记录差异，待引擎支持审批状态机 |
| XXXX-XX-XX | 执行视图 | 执行时树状展示当前步骤，完成后切换平铺，执行轨迹结构丢失 | 记录差异，待统一执行轨迹模型（树+状态+产物） |
| XXXX-XX-XX | 成本统计 | 会话页与成本页口径不一致、未按模型分组、无命中率视图 | 需求已明确：模型分组+口径统一+双树状图+命中率双线折线图，待实现 |
| XXXX-XX-XX | 停止按钮 | 点击停止无反应/有滞后，疑似中断链路未打通 | 待排查：事件绑定→前端取消信号→后端中断→进程 kill 全链路 |
| XXXX-XX-XX | 执行状态动效 | “执行中”无动效、位置在顶部与回复割裂 | 需求已明确：底部 sticky 状态条 + 动画 + 动作文案轮播 |
| XXXX-XX-XX | 斜杠命令 | 斜杠按钮点击无反应、无命令列表、不与输入框联动 | 待排查事件绑定/命令面板/输入框回填链路 |
| XXXX-XX-XX | 任务页签 | 任务时有时无、顶层项目层级不明、无法树状追溯 | 待排查任务持久化/过滤条件/多项目隔离/列表更新竞态 |
| XXXX-XX-XX | 页签标题 | 四页签顶部无对应标题、无“标题→内容”层级 | 需求已明确：左上角加粗页签标题，内容从第二行排 |
| XXXX-XX-XX | 删除会话清理 | 删除会话后缓存文件残留，重开 APP 重新加载已删会话 | 确定危害：重开加载+占存储；token 污染待引擎侧确认（用户理解不会） |
| XXXX-XX-XX | 目标模式/锁图标 | 锁图标用途不明、目标模式红圈难看、首次使用无引导 | 待排查：锁图标用途/目标模式逻辑/弹窗按钮合理性/帮助引导缺失 |

---

> 📌 本文档随每次发现新差距而更新，作为 Android 端能力建设的需求输入。

---

## 六、会话整体复盘（vibe coding 记录）

> 本会话完整脉络记录，供产品/引擎迭代参考。记录日期：XXXX-XX-XX

### 6.1 会话时间线

| 步骤 | 用户动作 | Agent 行为 | 结果 / 发现 |
|------|---------|-----------|------------|
| 1 | 要求调用 front design skill 生成麦肯锡级别汇报 HTML | 读取 30K+ 字符方案文档，提炼 18 页幻灯片结构，直接手写 HTML | 生成《数据治理体系建设与实施总体方案-汇报.html》（约 49KB / 18 页，含交互导航与打印适配） |
| 2 | 追问"你调用 skill 了吗" | 诚实承认未调用，工具列表无此 skill | 暴露差距①：无技能市场/动态安装能力 |
| 3 | 追问"为什么 Cloud 会自动搜索安装" | 解释 Android 版工具集固定、无技能发现机制 | 差距①细化（2.1~2.3） |
| 4 | 要求把差距记录成文档 | 创建《Android端与Cloud版Agent能力差距分析.md》 | 文档骨架建立（总览表+分析+建议+日志） |
| 5 | 询问 /plan 是否为计划模式 | 确认处于计划模式（只读审计），说明可用/禁用工具边界 | 引出差距④：计划模式仅禁用工具、无审批网关 |
| 6 | 反馈"打开计划模式与 Claude 不同" | 只读约束下备好内容，等用户关闭计划模式后写入 | 追加 2.4 计划模式机制不完整 |
| 7 | 反馈"执行中树状、完成后平铺" | 追加 2.5 执行过程可视化不一致 | 记录执行轨迹模型缺失 |
| 8 | 反馈"绘画页成本低、成本分析页成本高" | grep 项目确认无统计代码，追加 2.6 + 给出 7 条原因分析 | 记录成本统计口径问题 |
| 9 | 要求总结整个会话、更新问题现状 | 本次操作：追加第六章复盘 + 2.6 现状更新 | 文档形成完整闭环 |

### 6.2 本次会话核心结论

1. **能力差距清单（4 大类已确认）**：
   - skill 生态缺失（市场/扩展/匹配/对接）→ 🔴 高
   - Plan Mode 机制不完整（无规划→审批→执行状态机）→ 🔴 高
   - 执行过程可视化割裂（树状 vs 平铺，轨迹不可回溯）→ 🟡 中
   - 成本统计口径不一致（会话详情 vs 成本分析页）→ 🔴 高；已实测反推三价定价模型（2.6.1）+ 完整改进设计（2.6.2）
2. **Agent 侧应对策略**：无法动态安装 skill 时，用通用工具手动模拟 + 主动告知用户差距 + 记录到本文档形成需求输入
3. **用户侧价值**：本文档可直接作为 Reasonix 产品迭代需求输入——P0 内置高频 skill / 成本口径对齐；P1 计划审批状态机；P2 统一执行轨迹模型

### 6.3 遗留事项

| 事项 | 状态 |
|------|------|
| 成本统计改进实现（模型分组/三价口径/双树状图/命中率折线图） | 需求已明确（2.6.1 定价模型 + 2.6.2 设计），待引擎侧实现 |
| front design skill 的搜索/复刻（可尝试 web_search 找公开模板） | 未执行，可选 |
| 差距文档维护 | 持续更新中（本会话已记录 14 项差距 + 完整复盘） |
