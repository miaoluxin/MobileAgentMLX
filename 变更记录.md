# Reasonix Mobile 变更记录

## 2026-08-13（二十三批 · 思维链滚动聚焦修复 · 241 测试全绿）

用户反馈：展开思维链时列表自动对准头部（链头），不聚焦最新输出；流式输出中被"拉回去"，被迫手动往下拉。根因：列表唯二滚动点（ChatScreen LaunchedEffect(count) / LaunchedEffect(streamingText.length)）都是 `scrollToItem` **顶部对齐语义**——思维链展开 + 正文变长后末条 item 高度超视口，"滚到最后"退化为钉在链头；且无"用户是否接近底部"判定、纯推理阶段（streamingText 恒 0）零滚动、段详情框（220dp）无跟随。

修复：
- **滚到底部语义**（ChatScreen.kt）：三处滚动全部 `scrollOffset = Int.MAX_VALUE`（钳制到列表底部 = 最新文字/答案正文），不再顶部对齐
- **nearBottom 判定**（derivedStateOf）：最后可见 item 距末尾 ≥2 时不抢滚动——用户上滑查看历史/思维链时不再被拉回；回到底部恢复跟随
- **推理流式跟随**：新增 `LaunchedEffect(streamingReasoning.length)`（此前纯推理阶段零滚动，最新推理不可见）
- **段内跟随**（ThinkingTree.kt）：live THINKING 段详情框 `scrollState.scrollTo(maxValue)` 跟随最新推理，`isScrollInProgress` 保护手动滚动

---

## 2026-08-13（二十二批 · 全量自洽性审计修复 · 241 测试全绿）

用户要求全面检查代码/功能/提示词/缓存机制/工具/提示词-工具配合。三路并行审计（缓存+提示词 / 状态机+生命周期 / 工具+权限）产出去重后问题清单，按用户确认范围修复：**6 高级 + 20 中级全修 + 精选低级**（计划模式"可搜索不可写"白名单方案；百度 stub 移除入口）。

### 🔴 CRITICAL（6）
- **路径穿越**（FileBackend.resolveSafe）：`../` 越界、符号链接指向外部全部拒绝（此前 read_file("../x") 可读写工作区外，BASE"禁止工作区之外"承诺无代码落实）；新增 FileBackendTest（正常路径/越界/符号链接）
- **compact 孤儿 tool 消息 → API 400 风险**（ContextManager dropOldestNonUser）：删除 assistant 时连带删除引用其 toolCallId 的 tool 消息（恰好发生在上下文最紧张、最需要压缩的时刻）
- **计划模式只读被 shell/python_exec 架空**（PlanGate.writeBlocked 扩展）：PLANNING 阶段 shell/bash_output 走**只读命令白名单**（grep/find/cat/head/tail/ls/wc/sort/uniq/awk/cut/tr/diff/stat/file/sed（禁 -i）/git 只读子命令，禁重定向 >），python_exec 一律拒绝；isReadOnlyShell 纯函数 + PLAN_MODE_SUFFIX 声明白名单；并行路径（isParallelEligible）带 args 同步拦截
- **auto/yolo 绕过 DENY**（AgentEngine）：deny 规则优先于 autoApprove（默认审批模式即 auto，此前用户配的 deny 全部失效）
- **ALLOW_ALWAYS 规则 key 不匹配**：无 path 工具规则存 `tool`（与 decide 查 `tool` 统一），此前存 `tool:*` 永不命中，"总是允许"对 shell/python 形同虚设
- **回合事件跨会话污染**（G2）：TurnFinished/TurnAborted/Error/Delta/审批类事件补 sessionId，ChatViewModel 统一过滤（此前 A 会话中止会把 B 窗口会话整体替换成 A 的）

### 🟠 HIGH（20）
- **引擎状态机**：记忆回顾先清后注入（防无限累积）/ 熔断开路 exitStatus=FAILED / 进程树击杀竞态修复（取消路径先 registry.destroy 整树再 unregister，防孙进程僵尸）/ 计划审批反馈消息（[计划已批准] 等）纳入 isInjected 四处同口径 / **try 上移**（保存/技能提示/MCP 刷新悬挂点全部进 try，取消不再静默丢失）
- **ViewModel/存储**：snackbar 全链 tryEmit（防背压挂起引擎）/ handleEvent 异常防护（防 collector 死亡事件流永久静默）/ TaskStore list/save/update @Synchronized（防并行 shell 丢任务登记）/ denyRest 补发 DENIED 事件（防工具卡 RUNNING）/ compactManual 参数回退（消除"短 prefix"缓存风险）
- **子代理/MCP/工具**：语言跟随（子代理强制中文→跟随用户语言，对齐 BASE）/ 八要素补齐【返回形态】/ readOnlyTools 补 run_skill/read_skill/wait_job / MCP listTools 失败保留旧工具 + 工具名清洗 + readOnlyHint 解析自动放行
- **提示词小修**：PLAN_MODE_SUFFIX 白名单声明 / FILE_ATTACHMENT 5 文件上限声明 / 指令文件头部措辞去"优先级高于"歧义 / 技能注入 8KB 合计上限落实 / 32KB 口径降承诺精度 / 技能剧本重复删除（beiqifoton/gh-cli）

### 🟢 LOW（12）
choice schema 补 required + 2-4 选项校验 / planner 描述参数名统一 / 超时消息动态化 / 死代码删除（SkillStore.toInstructionText/userSkills、MiniJson 三函数、CommandRegistry.byId/CommandParam、EngineConfig/AppStore.projectName、ActiveTurn.streamingTail、ChatUiState.turnTokens）/ 未用 import / "已中止"文案统一"已停止"（通知栏/步骤树/回合状态/工具结果）/ 百度 stub 移除入口 / FactMemory IO 线程 + update 补锁 / planGates 会话删除回收 / abortAll 仅排队不假停止 / 双 abort 兜底竞态（seq 守卫）/ branch 补滤 [记忆回顾]+计划反馈

**测试**：新增 8 项（路径穿越 3 / 白名单 2 / DENY+key 2 / 孤儿消息 1）+ 更新受文案影响的 3 项断言，**241 全绿**。

---

## 2026-08-13（二十一批 · 子代理委派提示词标准补齐 · 233 测试全绿）

用户观察：Claude CLI 的系统提示词配合 agent 体系很强——尤其"给子代理自动分配任务时的高标准提示词"。三路调查（Android 现状 / PC 源码对照 / Claude Code 官方情报）确认：独立 suffix 架构与"何时派/何时不派"契约已同构达标，缺的是委派提示词标准模板与子代理本体契约条款。本次补齐（全中文，纯提示词层，不伤缓存）：

- **新增 DELEGATION_PROMPT_SUFFIX**（SystemPrompts.kt）：委派提示词八要素标准（【任务】【上下文】【约束】【期望输出】【质量标准】【禁止事项】【成功定义】【返回形态】）——子代理看不到父会话，委派 prompt 就是它的全部上下文，要求一次性给足；含"大任务拆多个可并行小子任务"纪律。独立 suffix 而非并入 BASE（缓存纪律：BASE+tools+指令文件前导字节不动）
- **接线**（AgentEngine.kt）：契约 + 委派标准合并传参（`subagentSuffix` 局部变量），**ratioUsed 两处与 buildMessages 三处调用点统一**（十二批纪律：ratio 估算与 buildMessages 同一完整参数）
- **子代理本体契约升级**（SubAgentManager.kt SUBAGENT_SYSTEM/PLANNER_SYSTEM）：对齐 Claude Code 契约 7 条 + PC DefaultTaskSystemPrompt——只做被派发任务不扩展目标 / 委派消息按八要素结构理解 / 信息不足明确说不确定或问精确问题禁止臆测 / 报告不直接展示给用户（结论式结构化、原始数据优先、禁表功式叙述）/ 完成即止 / 中文。保持静态常量（system 段前缀可命中，不恶化子代理侧缓存）
- **工具描述补模板**（AuxTools.kt subagent/planner）："按【任务】…【成功定义】组织"直接进 description（改 specs 指纹 → 一次 prefix 重建一次性成本）
- **测试**：新增 SystemPromptsTest（5 项纯字符串守卫：八要素齐全/独立 suffix 未并入 BASE/子代理契约条款/planner 格式保留/契约 when-when-not）
- 缓存安全：主会话前导命中不变；子代理 system 段命中不变；唯一一次性成本 = 工具描述变更导致的 specs 指纹重建

运行时能力（写型子代理、后台/续跑、fleet 舰队、嵌套 depth、结构化输出 schema、worktree 隔离）经调查已确认缺口，排期清单见计划文件，本次不做。

---

## 2026-08-13（二十批 · 停止慢 10+ 秒真根因修复 · 测试全绿）

用户真机反馈：装了最新版，点停止仍要等十几秒才停下来（PC 版立刻停）。**真根因不在 UI 反馈层，而在引擎取消收尾**——十九批的"链路通畅"判断漏掉了这里：

- **致命根因（Kotlin 取消语义坑）**：`AgentEngine` 的 CancellationException catch 里 `withContext(IO){save/capture/sync}` 和 `eventFlow.emit(TurnAborted)` 都在**已被取消的协程**里执行——取消后任何可取消挂起点立即再抛 CancellationException，`withContext` 连块体都不执行。结果是 catch 中途夭折：**TurnAborted 永远发不出去、被中止回合的会话永不落盘**，UI 唯一恢复途径是 15s 兜底强制复位——用户看到的"十几秒才停"就是兜底在兜。同时被中止回合的助手消息/工具结果一直没持久化（隐性数据丢失）
- **次要根因**：即使修好，emit 前串行落盘（save + 逐文件 checkpoint 快照 + 备份同步，SAF 慢）+ ViewModel 收事件后再读一遍整份磁盘会话，三层 I/O 门控 UI 复位

修复（对齐 PC 语义：先释放 UI，持久化后台完成）：

- **`AgentEngine.kt`**：取消 catch 重构——内存态快清理后 `withContext(NonCancellable)` 包裹：先 `emit(TurnAborted(session))`（事件改为携带最终内存会话，UI 免读盘即复位）→ IO 线程落盘（destroyAll + save + capture + sync）。killTree 的 /proc 读取 + ProcessBuilder.waitFor 阻塞也一并移出主线程
- **`AgentEngine.kt` teardownGate**：中止收尾落盘完成门闩（仅 Main 访问，无锁）；`runTurn` 开头 `teardownGate?.await()`——防"停止后立刻发新消息"读到旧盘上快照、随后覆盖保存导致被中止回合数据丢失
- **`ChatViewModel.kt`**：`abortAll()` 的 `abortCurrentTurn()` 移 IO 线程（防 killTree 阻塞卡"正在停止…"动画；cancel 仍同步在前，先 cancel 后 destroy 纪律不变）；TurnAborted 改用 `ev.session` 直接复位（抽 `resetAfterTurn` 与 TurnFinished 共用复位逻辑），不再二次读盘；15s 兜底保留作引擎真卡死时的最后防线
- 预期效果：停止 15s → **1 秒内**；"⚠ 停止超时"兜底不再出现；中止回合内容完整落盘

---

## 2026-08-11（十九批 · 停止按钮反馈修复 · 208 测试全绿）

用户真机反馈：执行中点击红色 ✕ 停止按钮 → 变转圈后一直转，停没停下不知道。排查确认中止链路本身通畅（审批/choice/计划审批 await 取消瞬时、shell 杀进程秒级、网络通常 2s 内 300s 兜底），问题在 UI 反馈层，三项修复：

- **InputRow 停止态加"正在停止…"文字**（ChatScreen.kt）：原只有裸转圈（ExpandedInputDialog 有文字、InputRow 没有——两处不一致），改为 转圈 + 文字，明确"正在停止"
- **aborting 超时兜底**（ChatViewModel.abortAll）：TurnAborted 事件异常未达时（引擎卡死等极端场景）15s 后强制恢复控制（running/aborting 复位）+ Snackbar"⚠ 停止超时，已强制恢复控制（未完成的修改保留）"——防转圈永不消失
- **停止完成文案"已中止"→"已停止"**（ChatViewModel.TurnAborted）：与"正在停止…"呼应

另：真机反馈"手动技能模糊名不识别"（frontenddesign 必须说全名 frontend-design）已排查确认四重断裂根因（精确匹配/模型不可见/run_skill 失败泄漏清单），修复方案已存档（计划文件），用户决定暂不实施。

---

## 2026-08-11（十八批 · 全量自洽性审计 · 2 CRITICAL + 2 HIGH + 7 MEDIUM 全部修复 · 208 测试全绿）

用户要求按十三批模式全量排查（代码/逻辑/提示词-工具配合/交互生命周期）。四路并行审计（缓存上下文 / 提示词配套 / 功能状态机 / UI 交互）+ 高风险点深读，发现并全部修复：

### 🔴 CRITICAL（2）
- **rewindTo 回合计数漏排注入消息**（ChatViewModel.kt）：回退判定只排除 [长期目标]，[记忆回顾]/[技能注入]（user 角色）被算进 turn → 检查点按错 turn 恢复，**文件回退错位/不完整**。修复：与引擎 isInjected 同口径三前缀排除
- **backfillTurns 漏排 [技能注入]**（SessionModels.kt）：旧/分支会话回合计数膨胀。修复：补前缀排除

### 🟠 HIGH（2）
- **off 技能模型不可见 + 缺失误报**：BASE【技能使用】补"用户点名/触发词暗示的手动模式技能用 run_skill 尝试调用"；gh-cli 触发词补 "GitHub"/"github"（防 missingSkillHint 把"用 GitHub 技能"误报缺失）
- **auto 模式 choice 声称-执行缺口**：BASE 声称"重大决策调 choice"但 auto/yolo 引擎直接放行 → 决策规则澄清"引擎审批自动放行时可直接执行，执行后如实汇报"

### 🟡 MEDIUM（7）
- **计划模式 + require 注入冲突**：注入文案按 planMode 附加只读提示（"先按剧本输出方案，勿执行写入"）——防剧本写入步骤与 planGate 拦截反复冲突
- **review 模式验证审批摩擦**：BASE 质量段补"验证工具被审批拒绝/不可用时记录未验证并继续，不重复请求被拒工具"
- **技能索引 4000 码点截断切半行**：截断落到最后一个完整行边界（模型不再看到残缺技能条目）
- **compact()/compactManual() Phase 2 死代码**（既有 bug）：谓词 `createdAt == first().createdAt` 对首元素恒真 → indexOfFirst 恒 0 → 兜底删除循环永不执行（上下文超阈值无兜底）。修复：按 `role != "user"` 查找（首条 user 天然保留标题语义）
- **outputStyle=json 裸 JSON 渲染误判**：风格文案补"JSON 部分务必用 ```json 代码块包裹"（防 MarkdownText 把 `*` 当斜体/数组当列表）
- **触发词重叠**：research "研究" 过泛（任何含"研究"任务命中 + 与 consulting-analysis"行业研究"双触发）→ 收窄为 调研/研究报告/研究一下/research
- **BASE 未提 run_skill 工具名**（发现链路缺口）：【技能使用】段补"匹配到技能后用 run_skill 加载执行、read_skill 查看详情"

### 🟢 LOW（4）
- **TodoSheet 执行中禁点无反馈**：加半透明（alpha 0.55）+ "执行中：清单由 Agent 管理，勾选只读"标注
- **gh-cli/beiqifoton 缺安装步骤**：gh-cli 补 "pkg install gh 后 gh auth login" 首步；beiqifoton 补 "pip install pandas openpyxl" 依赖步
- **branch() 携带孤立注入消息**：分支复制时过滤 [技能注入]（回合级临时上下文不进分支历史）
- **冗余清理**：ContextManager.buildPrefix 四行重复赋值删除

### 已验证无问题（审计确认）
- 模式选择器 focusable=false 全路径（点外/点输入框/点发送/返回键拦截均正确单次穿透）
- ToolCard 嵌套 SubAgentDetailSheet 无滚动冲突；WaitingResultHint 删除后视觉过渡连贯
- 回合状态无残留（TurnFinished/TurnAborted/Error 三路径完整复位）；技能禁用/删除即时生效
- 缓存键机制正确（Kotlin 结构相等非引用相等）；技能索引每回合重建 O(N) 可接受；require 注入清理/中止路径正确
- 注入消息计入上下文预算属正确行为（真实上送内容）

---

## 2026-08-11（十七批 · 技能体系三层加固 + 预置 11 个系统技能 · 208 测试全绿）

用户两个诉求：① 技能体系"提示词/索引/引擎"三层配合，防"技能越多越蠢"（不调用/过度调用）；② 把收集的 Claude Code 生态技能预置为系统技能（用户拍板推荐 11 个）。

### 28 个候选技能适配性分析结论（《别的软件好用的skills》）
- **❌ 桌面绑定 9 个**：agent-browser/electron（浏览器自动化）、local-asr/computer-use/img2img/mineru/ocr-npu/txt2img（全为 **Windows+Intel AIPC** 专属+特殊许可）、screenshot、obsidian-cli（需桌面 Obsidian）
- **🟡 需适配 8 个**：data-analysis（与内置重名跳过）、defuddle/slides/chart-visualization（依赖 npm/远程 API 跳过）、mlx-knowledge（硬编码 D:\ 路径跳过）、gh-cli/beiqifoton（提取精简后预置）
- **✅ 预置 11 个**（内容从 SKILL.md 剥离桌面依赖后精简提炼，中文剧本 120-350 字）

### 三层技能体系加固（防"技能越多越蠢"）
- **① 提示词层**（SystemPrompts.BASE 新增【技能使用】段 ~100 字）：任务拆解后先对照技能清单，**没有匹配就正常用通用工具不硬套**（防过度调用）；autoUse 语义 require 必须/prefer 优先/suggest 按需；inline 先加载成本低、subagent 确认才派（防浪费）
- **② 索引层**（SkillEngine.skillIndexBlock）：**按 autoUse 权重排序**（require=3/prefer=2/suggest=1）——技能增多后 4KB 截断只砍 suggest，高优先级永不因截断消失且排前更醒目（缓解注意力稀释）
- **③ 引擎层**（AgentEngine + SkillEngine.requireMatches）：**require 级强制注入**——任务触发词命中 require 技能时引擎直接把剧本注入下一条请求（`[技能注入]` 前缀，**先清后注入**防跨回合累积，对齐 GOAL_PREFIX 先例），不依赖模型自觉；isInjected 扩展识别新前缀防回合号污染；单技能 ≤4KB 合计 ≤8KB 码点安全截断

### 预置 11 个系统技能（BuiltinSkills.seventeenBatch）
- **索引组（suggest，5）**：test-driven-development（TDD 红绿重构）、writing-plans（实施计划）、consulting-analysis（32 框架咨询报告）、hivesql-lineage-analysis（字段级血缘）、brainstorming（创意前设计审批）
- **点名组（autoUse=off 不进索引，6）**：frontend-design、internal-comms（3P/通讯稿/FAQ）、theme-factory（10 主题）、obsidian-markdown（双链/callout）、beiqifoton-financial-report-assessment（车企财务人天评估）、gh-cli（命令速查）
- 索引容量核算：9 内置 + 5 新增 = 14 个进索引 ≈1000 字 < 4KB 预算 ✓

### 测试
- 205 → 208：skillIndexBlock 排序（require/prefer 恒在 suggest 前）、requireMatches（仅 require+触发词命中）、十七批内置技能存在性 + off 不进索引

---

## 2026-08-11（十六批 · 系统提示词原则式重构 · 205 测试全绿）

用户核心反馈：移动端系统提示词比 PC 版 reasonnix 和 Claude CLI "蠢"——不够泛化、不够智能。三路探查（移动端逐段审计 / PC 源码对照 / 历史迭代梳理）定位六根因后全量重构：

### 根因（为什么"蠢"）
1. **源头是命令清单**：BASE 源自建议稿（命令式文档），历次迭代只修"声称未兑现"，"泛化与智能"维度从未重构
2. **认知层缺失**：PC tokenDeliveryPrompt 质量契约（有证据才声称成功/声明未验证假设/完成即停止/修改后验证/渐进探索）移动端全无
3. **踩坑笔记占位**：heredoc/curl 退出码 23/28/f-string//tmp/venv 200+ 字环境笔记堆进 BASE（shell 描述已有半数，重复）
4. **硬编码不泛化**：PLAN_MODE 写死 5 工具名、4 并发、flash/pro 代号
5. **自相矛盾**："不可逆先说明再执行" vs "调用 choice 让用户选择"
6. **恒定浪费**：BASE 与工具描述重复 ~300-500 字/回合

### 重构（SystemPrompts.kt）
- **BASE 2200 字 → ~750 字原则式 7 段**：身份 / 工作方式（语言自适应"跟随用户语言+代码不翻译"替代硬编码中文、执行纪律保留、修改后验证、渐进探索）/ 质量与诚实（**新段：有证据才声称成功、不臆造、完成即停止、模糊需求先假设后声明**）/ 决策规则（修矛盾：无安全默认值→choice、可逆小决策合理默认直接执行）/ 完整环境（瘦身 3 行，细节指向工具描述）/ 任务规划与进度（保留已验证纪律压缩）/ 输出截断意识
- **SUBAGENT_CONTRACT_SUFFIX 850 → ~280 字**：删收益框架/4 并发/模型代号（subagent/planner 工具描述已含，去重）；保留 when/when-not/自包含纪律
- **PLAN_MODE_SUFFIX 泛化**：写工具硬编码清单 → "任何写入类工具（引擎会直接拒绝）"；**补被拒行为**："被拒后停止尝试写入，将修改并入方案等待批准"

### 工具描述下沉（PC 哲学：工具描述即工作流指南）
- **shell**：下沉 heredoc（python3 <<'EOF' 非 -c 单引号）/ curl 退出码 23/28 / 长命令 >120s 用 bash_output；保留 venv 激活//tmp/专用工具说明（原 5 条注意事项编号化）
- **python_exec**：补"依赖缺失 pip install 自装；输出大打印摘要"
- **complete_step**：补证据契约"summary 附完成证据（验证输出/测试结果/检查结论）"（对齐 PC complete_step 哲学，引擎纯汇报行为不改）
- **read_file 不改**：无 offset/limit 参数，"分页"声称不成立（配套一致性原则）

### 配套一致性复核（十二批原则延续）
- 新 BASE 所有声称与工具/引擎行为逐一核对（web_fetch 放行、32KB 截断、choice 弹窗、工具自动修复、todo 分工）；"修改后验证/完成即停止"为行为引导非能力声称，引擎零改动
- ContextManager 拼接与缓存键逻辑不动（BASE 常量引用不变）

### 测试与验收
- 205 测试全绿（SystemPrompts 无文案断言；ContextManagerTest 只测拼接/缓存键）；APK 构建成功
- 真机验收重点（行为变化点）：① 多步任务改完主动验证；② 模糊需求声明假设；③ 不可逆操作走 choice 或声明；④ 完成后自然停止；⑤ 多主题并行派 subagent；⑥ 计划模式被拒后停止写入并入方案
- 缓存：BASE 改动触发一次 prefix 重建（一次性预期成本）

---

## 2026-08-11（十五批 · 会话页四问题反馈修复 · 205 测试全绿）

用户反馈四个问题：① 思维链有"未命名段落"；② 执行中展开 todolist 可手工点击打勾（诧异）；③ 键盘展开时点模式选择器键盘自动收起（一跳一跳）；④ 实时状态栏很棒但同屏展示进展/任务的地方太多。四路定位后全部修复：

### ① 思维链兜底命名（ThinkingTree.kt）
- 推理无任何结构（序号/中文序号/第X步/标题正则均未命中）时兜底单段 header 为空 → 显示"（未命名段落）"；改为固定标题"思考过程"（新常量 FALLBACK_HEADER），渲染端 ifBlank 防御回退同步

### ② 待办执行中禁点（TodoSheet.kt）
- 任务项点击勾选原全时段可用，直接写 Agent 共享的 todoStore JSON（执行中误触会改坏 Agent 维护的进度）；改为 `.clickable(enabled = !running)` —— 执行中只读展示，Agent 空闲时可手动勾选

### ③ 模式选择器不再收起键盘（ChatScreen.kt）
- 根因：M3 DropdownMenu 内部 PopupWindow 默认 focusable=true，弹出抢走窗口焦点 → 输入框失焦 → IME 自动收起（全库无主动 hide 代码）；adjustResize 放大跳动感
- 三个菜单（⚙模型 / 💭思考 / 🛡审批）加 `PopupProperties(focusable = false)`：弹窗不抢焦点、键盘保持展开不跳；非 focusable 弹窗不拦截返回键 → BackHandler 兜底关闭

### ④ 执行态重复展示中度整合（状态栏为唯一实时总览）
- **TodoSheet 删除"当前工具/子代理"块**（与 AgentStatusBar 同源 100% 重复）；子代理"查看执行全程"入口移入消息流工具卡（SubAgentDetailSheet 直接挂载于 ToolCard）
- **删除流式区"等待执行结果"提示**（底部任务栏波浪点+动作行已承担该语义；WaitingResultHint 及②动效注释移除）
- **AgentStatusBar 状态行去掉 ↓tokens**（tokens 统一由顶部 ContextBar 展示；statusBarStateLine 两参化）
- 清理死代码：toolStatusMark / agentStatusLine / subagentProgressText 及对应测试删除；ExecutionFx 无用 UiFormats import 清理

### 测试
- 208 → 205：ThinkingTreeTest 兜底标题断言更新（"" → FALLBACK_HEADER）、statusBarStateLine 两参断言、删除 3 项死代码测试

---

## 2026-08-10（十三批 · 全面自洽性审计修复 · 195 测试全绿）

用户要求"仔细排查所有逻辑是否自洽（功能/提示词/工具/交互/DeepSeek 缓存）"。四路并行审计（缓存逻辑/提示词一致性/功能状态机/交互生命周期）共发现 1 CRITICAL + 4 HIGH + 6 MEDIUM + 若干 LOW，全部修复：

### DeepSeek 缓存逻辑（5 项）
- **specsKey 短路优化**：非 specs 参数先判（O(1)），相同才计算工具指纹 —— 原来每轮迭代无条件 stringify 全部工具参数
- **specsKey 按名称排序**：与 prefix 实际拼接的 sortedBy 一致 —— 否则 MCP"先卸后注"改变插入顺序导致假 miss（内容字节相同仍每回合重建 prefix）
- **estimatedTokens/ratioUsed 全参数**：首轮构建带 instruction/suffix —— 原来首轮"短 prefix"被缓存为 builtPrefix 且 prefixVersion 虚增
- **normalizeMcpSchema HashMap→LinkedHashMap**：key 序可预测，防 MCP schema key 序变化导致 prefix 字节漂移（缓存全量失效）
- **DeepSeekClient tools 按名称排序**：与 prefix 一致 —— tools 数组顺序漂移会截断服务端自动缓存命中

### 功能状态机（5 项）
- **【CRITICAL】runTurn 非取消异常兜底 catch**：此前穿透导致 PlanGate 死锁（卡 PENDING_REVIEW）、approvals/choices 泄漏、进度不落盘、UI 卡 running —— 兜底统一收尾（FAILED + 清理 + 落盘 + Error 事件，不 rethrow）
- **【HIGH】超时精确杀进程**：destroyAll→destroy(callId)；ShellTaskRunner 加 taskIdOverride，shell/python 用 callId 注册 —— 原 destroyAll 会误杀并行组其他工具进程
- **【HIGH】multi_edit 全路径**：新增 extractPaths —— 原只取第一个 edit 的 path，检查点快照/磁盘回写漏文件
- **【MEDIUM】错误 break 标记 FAILED**：步数/预算/Key 缺失三处 break 恒 SUCCESS → exitStatus 变量
- **【MEDIUM】TodoStore/FactMemory @Synchronized**：引擎 IO 与 UI 并发 read-modify-write 互相覆盖丢数据

### 提示词-工具（3 项）
- **【HIGH】update_goal 注入修复**：removeAll 旧 GOAL_PREFIX 再注入 —— 原只查"是否存在"，中途变更目标被旧消息遮蔽、清除目标旧消息残留
- delete_range 描述补"不可逆操作"标注（对齐 move_file）
- MCP 措辞改"按需调用（review 审批模式下首次调用可能需用户确认）"（原"直接调用"与实际审批行为矛盾）

### 交互（3 项）
- **SubAgentStream/ToolOutput 加 sessionId 过滤**：分屏另一窗口的流不再污染本窗口缓冲（原仅 ToolStatusChanged 有过滤）
- **flush 时 8KB/4KB 截断**：原只截显示不截 state，长输出全量驻留内存
- **CrashLogDialog 清空即时刷新**：remember 快照 → mutableStateOf

---

## 2026-08-10（十二批 · 提示词-工具配套对齐（致敬 Claude CLI）+ 子代理执行过程可视化 · 195 测试全绿）

用户要求：① 系统提示词声称的能力与工具注册表、引擎行为三方必须配套（对照 Claude CLI 系统提示词与工具调用的一切配合，逐条核对）；② 子代理执行全过程可见可查（现状黑盒：60-300s 只有状态点转圈）。双路探索审计后确认：3 处"声称但未兑现"P0 差异 + 关键缓存键缺陷 + 子代理 reasoning 被丢弃。

### Q1 配套对齐（提示词 ↔ 工具 ↔ 引擎）
- **修正虚假声称**：code_index（"建立索引后续更快"→实为一次性扫描）、complete_step（"与 todo 清单联动"→实为纯汇报）——SystemPrompts BASE 与 PcTools 描述同步对齐
- **补工具指引段**：multi_edit/delete_range/move_file、bash_output+wait_job+kill_shell 三件套、技能类 run_skill/read_skill/install_skill + explore/research/review、todo_complete 与 complete_step 分工（工具清单本身 prefix 已自动注入，缺的是"何时用"）
- **MCP 真实参数 schema 注入**（实现级修复）：McpClient 保留 inputSchema（mapTool 纯函数可单测）；McpToolBridge 直接铺开真实属性（保留 required/enum/嵌套），normalizeMcpSchema 纯函数（剔除 $schema/$id/title/additionalProperties/definitions，规模上限 4KB/40 props 超限回退通用壳）；修复后模型能正确填 MCP 参数（此前盲传 arguments）
- **ContextManager 缓存键指纹**（关键新发现）：原 specsKey 只比工具名 → 运行时 description/parameters 变化（MCP schema 每回合"先卸后注"）被短路缓存吞掉；改为 名字|描述|参数指纹
- 踩坑：**字符串模板里 `"$schema"` 被 Kotlin 当变量引用 → 必须 `"\$schema"` 转义**（编译错误 "Unresolved reference 'schema'"）

### Q2 子代理执行过程三层联动可视化
- **数据链路**：SubAgentManager.runLoop 加 onDelta(content, reasoning) 回调（此前 reasoning 被丢弃；子代理默认开思考模式 30-120s 无正文 → 黑屏风险）；ToolContext 加 onSubAgentDelta 字段（默认 null 零破坏）；AgentEvent.SubAgentStream(callId, content, reasoning) 单事件双字段；buildContext 接线 tryEmit；SubAgentTool/PlannerTool/RunSkillTool(subagent 技能) 全透传
- **状态层**：ChatUiState.subagentStreams: Map<callId, SubAgentStreamState>；纯函数 appendSubAgentDelta（8KB/4KB 尾部截断）/subagentProgressText（思考中…/输出 N 字）/applySubAgentDelta；按 callId 分区缓冲 + 统一 100ms flush（4 并行不放大重组）；回合开始/结束清空不入 session（完成后结果走 resultText 既有路径）
- **UI 三层**：① TodoSheet 子代理行加进度文案（"· 思考中…"/"· 输出 N 字"）+ "查看 ▸" 点击入口；② SubAgentDetailSheet 新弹窗（任务描述摘要 + 实时思考链尾部 + 实时正文 + 完成后最终结果）；③ 消息流 ToolCard 执行中展开区实时流（💭 reasoning tail + content 光标）
- 测试 192 → 195（子代理流纯函数 3 项）

---

## 2026-08-10（十一批 · 状态栏展开崩溃修复 + 子代理契约强化 · 183 测试全绿）

用户反馈重大 bug：子代理执行中点击输入框上方状态栏展开任务清单 → App 静默退出，无任何日志。双路探索 + 逐文件核对定位：① **Todo id 同毫秒碰撞**（`t${System.currentTimeMillis()}`）→ TodoSheet LazyColumn key 重复 → IllegalStateException，时机恰在"点击展开"（同款 bug 事实记忆已修过）；② **主线程同步大 I/O**（sessionStore.save/load + checkpointStore.capture + syncAndBackup 全在 Main）→ 子代理回合会话 JSON 膨胀 → ANR → 国产 ROM 静默杀进程；③ **零崩溃日志基础设施**（无 UncaughtExceptionHandler）——一切崩溃=静默退出无法取证。

### 崩溃修复
- **Todo id 唯一化**：`newTodoId()` 毫秒+纳秒（AuxTools.kt，对齐 FactMemory 先例）；list 读取反向去重防历史重复 id（保留最新一条，注释说明）
- **I/O 全部下放 IO 线程**：AgentEngine 三处 sessionStore.save + checkpoint capture + syncAndBackup 合并入 `withContext(IO)`；ChatViewModel reloadSession 改 suspend+IO（turn 结束读盘不再卡 Main）；todoAdd/todoToggle/TodoUpdated 的 todo 文件读写移 IO
- **全局崩溃日志**：新模块 `core/diagnose/CrashLog`（UncaughtExceptionHandler → filesDir/crashlogs 落盘，含机型/版本/堆栈，毫秒时间戳命名，保留 5 份轮转，链回系统默认 handler 不改变退出语义）；设置页"关于"区可查看/清空——今后任何崩溃都有日志可查
- **runBlocking 治理**：审批规则持久化 runBlocking(IO)→GlobalScope.launch(IO)（对齐 UpdateGoalTool 先例）；BashOutputTool runBlocking 核实运行于引擎 IO 上下文无死锁，加注释说明

### 体验（回应用户"想展开看任务情况和子代理情况"）
- **TodoSheet 新增"当前工具 / 子代理"区块**：实时显示 activeTurnTools（倒序最新 8 条，状态点 ⏳/✓/✗/✕/⏸ + 子代理/规划者角色 + 意图文案；"● N 个并行子任务"汇总行），执行中状态随工具实时变化（纯函数 toolStatusMark/agentStatusLine 可单测）

### 子代理契约强化（对齐 Claude Code 委托设计）
- **SystemPrompts 独立 `SUBAGENT_CONTRACT_SUFFIX`**（不再并入 BASE）：何时派发 4 信号（多独立主题/广撒网研究/跨域汇报/用户要全面）/ 何时不派 3 边界（单点查询/需亲执工具/子代理间依赖）/ 收益框架（不占主上下文+引擎并行 4+只综合）/ 执行纪律（同批并发+prompt 自包含+不转发原文）
- **SubAgentTool/PlannerTool description 重写**（模型对 schema 描述重现度最高）：when 信号 + when-not + prompt 自包含要求直接进工具描述
- ContextManager.buildPrefix/buildMessages 加 `subagentSuffix` 参数（默认 null，测试零破坏），引擎调用处传入

### 测试
- 175 → 183：AuxToolsTest（id 唯一性/往返/旧格式重复 id 去重）、CrashLogTest（落盘/轮转/坏目录兜底）、RunningActionLabelTest 追加 toolStatusMark/agentStatusLine
- 踩坑：`distinctBy` 保留首次出现——要"保留后者"须 reversed 去重再 reversed；崩溃日志文件名秒级粒度同秒覆盖 → 毫秒

---

## 2026-08-10（十批 · 执行可见性 + 流畅度 + 审批放行 · 175 测试全绿）

用户反馈三大问题（三路探索定位根因）：① 发一句话后就停住、看不到工具执行情况；② 手机比 PC 卡（同一 API）；③ Agent 只说没做。根因：工具折叠条执行中默认折叠（工具在执行但 UI 不可见）、思考阶段 30-120s 黑屏（PC 流式显示 reasoning，手机只剩"思考中"三个字）、流式 UI 无节流（每 1-5 字符一次全 UI 重组）、每 token 触发滚动动画、**默认审批模式 review（写工具要审批，没点就无限挂起=停住）**、SAF 是真机默认路径（比 RealBackend 慢 10-50 倍）。

### P0 展示（执行可见性）
- **工具折叠条执行中自动展开**：任一工具 RUNNING 时强制展开（LaunchedEffect 监听），实时看到旋转/状态点/意图；完成后保持用户状态
- **思考链流式展示**：StreamingBlock 思考阶段实时显示 reasoning 尾部（"💭 思考中 + 内容"滚动，对齐 PC 版流式渲染 reasoning——DeepSeek 思考 30-120s 不再黑屏）；有正文后切正文
- **ToolCard RUNNING 默认展开详情**（参数可见）+ **实时输出渲染**（toolOutputs 数据此前存了无 UI 消费——shell 日志现在执行中可见）
- **滚动 key 剥离**：animateScrollToItem 只按消息数触发（原来每 token 触发 300ms 动画）；流式正文改无动画直跳 + 200ms 节流跟随

### P0 性能（流畅度）
- **流式 debounce**：AssistantDelta/ReasoningDelta 不再直接更新 StateFlow——VM 内 StringBuilder 累积 + 100ms 批量 flush（1000 token 重组从 200-1000 次降到 ~10 次）

### P0 审批（只说没做/停住）
- **默认审批模式 review → auto**（工具调用默认放行，对齐用户预期）；设置页加说明文案（Auto 放行/Review 逐个确认 60s 超时自动拒绝/Yolo 全跳过）
- **审批挂起 60s 超时**：awaitApproval/awaitChoice 超时自动拒绝/取消 + 提示（防无限挂起"停住"）

### P1 性能
- **glob 定向遍历**：globRootPrefix 纯函数提取 pattern 静态前缀（"src/**/*.kt"→"src"）作为 listTree 起点，避免全树扫描（SAF 下省 50-80%）
- SAF→Real 提速引导文案（设置页审批区同步说明）

### 其他
- 测试 170 → 175（globRootPrefix 5 项）；踩坑：**KDoc 注释里不能写 "**/"（块注释提前终止导致语法错误）**——用文字描述代替

---

## 2026-08-10（九批 · 自动并行子任务 + 粘贴长文折叠 · 170 测试全绿）

用户看到 Claude Code CLI 两个特性要求对齐：① 复杂任务自动发起多个并行子任务（"● 3 background agents launched"）；② 粘贴长文折叠（"【已粘贴文字 +N行】"）。

### 自动并行子任务
- **只读放行**（PolicyEngine readOnlyTools 加 subagent/planner）：review 模式自动放行 + 入并行组 —— 模型同轮调多个 subagent 工具，引擎 ToolBatchRunner 自动并发执行（无需新编排层，结果注入链路已有）
- **超时豁免**：timeoutFor(name) 映射——subagent/planner 300s（子代理单次 API 60-300s），其余 120s；**修复"子代理 120s 被超时杀掉"现状 bug**
- **提示词**：子代理契约段补并行引导（"需要同时研究多个独立主题时，同一次回复中同时调用多个 subagent，每个负责一个独立子问题；引擎自动并行（最多 4 并发）；全部完成后综合成最终答案"）
- **UI**：parallelSubagentCount 纯函数 + statusBarAction —— 并行子任务 ≥2 时底部任务区显示"● N 个并行子任务"（对齐 Claude CLI 视觉），消息流展开工具折叠条后每个子代理独立行（状态点 ✓/✗ + 意图文案）

### 粘贴长文折叠（阈值 500 字符/15 行）
- **pasteCollapseInfo 纯函数**：input 增量超阈值判定为粘贴（正常打字/删除远小于阈值；中文逐字计数），返回"📋 已粘贴文字 +N行（M 字符）"
- ChatScaffold：pastedFullText/pastedSummary 状态（remember 非 saveable——大文本进 Bundle 有 TransactionTooLarge 风险）+ LaunchedEffect 检测；折叠后输入框清空、全文存 pastedFullText
- InputRow：折叠态显示摘要卡片（📋 摘要 + ▸ 预览 + ✕ 删除）替代输入框；⬍ 最大化隐藏
- PastePreviewDialog：滚动查看全文 + [发送全文] [展开编辑] [删除]；展开编辑回填前同步 previousInput 防重复折叠
- sendInput：`textToSend = pastedFullText ?: input` 发送全文，发送后清折叠

### 其他
- 测试 161 → 170（PasteCollapse 7 + parallelSubagentCount/statusBarAction 2）

---

## 2026-08-10（八批 · 全量审查修复 · 161 测试全绿）

用户要求全量审查：逻辑/交互/功能/系统提示词/工具链路。三路并行审查（引擎 39 项/UI 14 项/提示词能力 20+ 项），确认闭环：事件流 16 种全消费、PlanGate/审批/choice 全链路、工具注册-提示词-UI 三层对齐（35 工具）、记忆/技能/MCP/进程链路。修复如下：

### P0 真缺陷
- **PythonExecTool files 参数未接线**（高）：parameters 声明 files 但 execute 不读 → 删除参数 + 描述改"脚本在项目真实根目录以相对路径访问文件"
- **RepairPipeline contentText 死参数**（高）：recoverDetailed 第三参全程未用 → 移除（引擎/测试同步）
- **ApprovalSheet 裸英文工具名**：主文案改 toolActionLabel + 工具名降小字（对齐 ToolCard 模式）
- **approvals/choices 悬挂**：abort 时 CompletableDeferred 残留 map → 取消分支 clear
- **read_skill 重复分支**（when 不可达死代码）：删除
- **色值系统统一**：Theme.SuccessGreen 0xFF22C55E→0xFF34A853（代码实际用值）；CostCharts 成本绿同步；ChatScreen 两处 0xFFF5A623→0xFFF59E0B（对齐 WarnAmber）
- **审批规则不持久化**："始终允许"重启丢失 → PolicyEngine.onRulesChanged 回调 + ReasonixApp 装配 saveRules(runBlocking IO 低频) + 启动 loadRules 恢复
- **DeepSeekClient GlobalScope 重试泄漏**：重试协程脱离流生命周期 → 改 callbackFlow producerScope launch（流关闭自动取消）

### P1 增强
- MAX_STEPS 30→100（预算/熔断/无工具 break 防失控）
- 提示词补 3 工具引导：code_index（大型工程先建索引）/ complete_step（步骤标记）/ update_goal（目标模式）+ MCP 说明
- 提示词 4 处文案修正：search_files 定位（grep 内容/glob 路径/search_files 文件名）、python_exec 无需 heredoc、截断修复声明如实、工具选择偏好重构
- **ContextManager buildPrefix 缓存短路**：输入未变直接返回已构建前缀（回合迭代省 20-30KB 重建；工具集按名称列表比较——all() 每次新实例）
- Storm 去重 key 改完整 argsJson（消除 hashCode 碰撞误删）
- planModeBlocksWrite 注释标注测试专用（防误用替代 PlanGate）

### P2 小修
- 设置页思考模式标签如实：快速（不思考，最省）/ 标准 / 深度；EngineConfig 注释补 explanatory/learning
- temperature 注释修正（不传=模型默认）
- completeOnboardingDone 空函数删除

### 不做（记录）
SafBackend runBlocking（已在 IO 线程）、并行跨写工具重排序（依赖风险）、preview/notebookedit/delete_symbol/gitignore 工具（移动端合理取舍）、SessionCascade runBlocking/ModeBar 抖动/发送按钮风格/MarkdownText 跨包（低价值）

---

## 2026-08-10（七批 · 底部任务区 + 历史收敛 · 161 测试全绿）

用户反馈：① Claude CLI 执行时会话最下方常驻任务区（当前任务/待办/状态行/完成折叠），我们的 todo 藏在弹层里；② 执行完毕后历史消息平铺工具调用和工具日志，用户只要结果；③ 会话界面整体以 Claude 样式为主合并精简。

### 底部任务区（AgentStatusBar，替换 ExecutionIndicatorBar）
- 执行中常驻输入框上方：当前任务 ◼（CurrentTodoRow 脉冲高亮）+ 状态行 `5分55s · ↓12.3k tokens · 正在读取配置文件`（对齐 Claude "5m 55s · ↓ 51.8k tokens · thinking"）+ 未完成待办 ◻（最多 4 条超出折叠）+ 完成项折叠 `… +N completed` + 排队指令（可逐条取消）
- 无待办退化为动作条（spinner+动作+计时）；点击整块打开 TodoSheet
- 纯函数可单测：`statusBarStateLine`（耗时·token·动作）/`visibleTodos`（分组+折叠数）/`doneCount`
- ChatViewModel：ChatUiState.turnTokens 本回合 token 累计（startTurn 清零 + CostUpdated 累加）

### 历史收敛（只要结果）
- assistant 消息工具卡：`m.toolCalls.forEach { ToolCard }` → `⚙ 工具调用（N）▸` 折叠汇总条（点击展开）
- tool 角色结果行（ToolResultLine）不再渲染（结果在工具卡展开区已有）；死代码 ToolResultLine/clickableText 删除
- TurnTreeCard 默认折叠（原展开）
- **删除 TurnOverviewCard**（执行中总览卡）：其能力逐一归属——头部摘要/任务清单/工具链→AgentStatusBar；终止按钮→InputRow 已有；思考链→Claude 风格不展示（状态行 thinking 由底部承担）；排队→AgentStatusBar 排队行；MessageList scrollToItem 偏移 +2→+1
- 执行中界面 = 历史（收敛）+ 流式正文 + 底部任务区 + ModeBar + InputRow（对齐 Claude CLI 布局）

### 其他
- 测试 158 → 161（statusBarStateLine/visibleTodos/doneCount）

---

## 2026-08-10（六批 · 执行保活 + 提速 + 渲染对齐 Claude CLI · 158 测试全绿）

用户反馈：① 让 Agent 分析+写代码要等 1-2 小时，App 切后台睡觉醒来还没写完；② 会话界面把工具名（shell/read_file）裸露给用户，用户要"正在做什么"而不是"调了什么工具"，参考 Claude Code CLI 渲染。

### 后台保活（切后台照跑 + 通知可见）
- **AgentKeepaliveService**（新）：FGS(dataSync) 保活锚点 + PARTIAL_WAKE_LOCK（2h 兜底）+ 通知实时"正在执行：<意图>"；回合结束 IDLE 宽限 2s（队列续回合取消停止）→ "执行完成/已中止"通知 → 停服
- **ActiveTurn.kt**（新）：ActivePhase/ActiveTurnStatus StateFlow 快照 + notificationText/intentText/fmtSecs 纯函数；引擎各阶段更新（回合开始/流式/工具 RUNNING/审批·choice·计划审批挂起/结束取消）
- Manifest：POST_NOTIFICATIONS（Android 13+ 运行时请求，被拒不阻塞）+ WAKE_LOCK + REQUEST_IGNORE_BATTERY_OPTIMIZATIONS（引导对话框 + AppStore batteryPromptShown 持久化只提示一次；ACTION_REQUEST 失败回退设置页）
- 规避 Android 12+ 后台禁止启动 FGS：仅首回合启动，续回合复用；通知 id 11/12 与 TaskService(1/2) 错开
- ChatViewModel：startTurn keepaliveStart；完成 snackbar"✓ 回合完成 · 耗时X"；**ToolStatusChanged 按 sessionId 过滤（顺带修复跨会话事件泄漏缺陷）**

### 执行提速
- **ParallelPlanner.kt**（新）：isParallelEligible（非交互/非写/计划不拦截/自动放行）+ parallelGroupSpan（最大连续段，≥2 才成组）+ ToolBatchRunner（上限 4 分块并发，结果按输入序）
- AgentEngine：for→while(i)；整组 ①串行先发 RUNNING → ②并发 → ③按原序回填；applyResult(preResult) 防重复执行（自查发现：初版并行组二次 doExecute 会导致 todo_add 双写）；熔断补 DENIED 索引修正
- PolicyEngine readOnlyTools 加 todo_*（review 模式任务骨架不再每次弹审批）

### 渲染对齐 Claude CLI（意图优先）
- 提示词：执行纪律"先一句意图描述→立即调工具"（保留反懒惰）；todo 文本规范；TodoAdd.description 同步
- 数据：ToolCallRecord/StepRecord 加 intent（旧 JSON 缺省空串兼容）；ToolStatusChanged 加 intent+sessionId（默认值零改动）
- 引擎：批次 intent = 调用前正文（intentText 只取 contentText 不取 reasoningText）→ 首个工具携带
- UI：toolActionLabel 统一映射 21 类（read_skill 先于 startsWith("read")）；runningActionLabel intent 优先；总览头部"⚡ <意图>"；工具链/ToolCard 折叠单行（意图主 + 工具名小字 + 点击展开）；步骤树意图主显

### 其他
- 测试 134 → 158（ParallelPlanner 8/ActiveTurn 6/RunningActionLabel 7/SessionStore intent 2/TurnTracker intent 1）

---

## 2026-08-10（五批 · 二次审查修正：提示词/自动化 PC 对齐 · 134 测试全绿）

二次全面审查（含系统提示词与 Agent 自动化程度 PC 对照维度）：

### 代码缺陷
- **sendInput 双重执行（高）**：面板点击执行后回填，未编辑直接回车 → 二次执行（/plan.toggle 切回原状态、/goal 弹两次）→ lastExecutedCommand 标记，同命令回车只清空不重复执行
- URL 重装已禁用技能 → 自动重新启用（重装 = 重新启用意图）

### 提示词 PC 对齐（自动化程度）
- **记忆陈旧性声明**：`[记忆回顾]` 前缀加"可能已过时，仅作背景参考——使用时请现实验证"（对齐 PC memory.Compose）
- **技能索引调用哲学**：header 加"inline 技能加载成本低，可能相关即可先加载；subagent 较重确认需要才委派"（对齐 PC skill.ApplyIndex）
- 语言自适应评估为不做（当前中文用户固定，per-turn 注入需 BASE 分层重构才有缓存意义，记录差异）

### PC 特性对齐（用户确认全做）
- **choice 升级为 PC ask**：多题 questions（1-4 题，header 分组/multiSelect 多选/recommendedFirst 推荐前置）+ 单题向后兼容；引擎 ChoiceQuestion 结构 + UserChoiceRequired 多题事件 + respondChoice 返回每题选中集合；ApprovalSheet ChoiceCard 多题渲染（分组/复选/推荐⭐/统一提交）
- **技能顶级工具**：explore/research/review 独立 ToolSpec（对齐 PC boot.go），只读自动放行，返回内置技能剧本 + 任务参数
- **输出风格补两种**：explanatory（解释性）/ learning（协作学习，留 TODO(human)），设置页 chips 五项

### 审查确认正确
PlanGate per-session 一致性；BudgetWarning 分支顺序；Skill disabled 全链路；余额刷新；scrollToItem 时序；面板自动唤出；熔断补步骤；计划模式全链路

---

## 2026-08-10（四批补充 · 全面审查修正 · 134 测试全绿）

对三批 14 项改动做三维度全面审查（引擎逻辑/UI 一致性/PC 版对照），修复确认的缺陷并对齐 PC 版特性：

### P0 缺陷修复
- **PlanGate 跨会话污染（严重）**：改为 per-session 隔离（AgentEngine.planGateFor(sessionId)，ConcurrentHashMap）；会话 A 计划审批不再被会话 B 回合破坏
- **submit_plan 非计划模式误调用**：phase 前置检查（非 PLANNING 返回 null → 引擎记 FAILED 不挂起）
- **planReady 残留**：reloadSession/Error 清除（防审批层卡屏）
- **aborting 残留**：Error 处理 + startTurn 防御性复位
- **黑名单异步写入**：SessionCascade 改 runBlocking 同步（防进程被杀复活窗口）+ 备份删除返回值检查
- **回填误发送**：onSend 斜杠本地分发（回填/输入的 /命令 回车即执行，不发给模型）+ 回填后 FocusRequester 焦点
- 其他：endTurn isCompleted 防护 / 熔断 break 补未处理工具步骤（DENIED）

### P1 一致性与 PC 对齐
- 计划批准注入 plan 原文（对齐 PC Executor）；PLAN_MODE_SUFFIX 追加分层计划指令（对齐 PC Marker）
- 金额精度统一（¥0.01/¥0.010/¥0.0100 → UiFormats.usd 规则）；折线图 Y 轴标签按视图模式动态
- TurnTreeCard 补 diffText；prunedOnce 改 rememberSaveable；技能索引码点安全截断
- theme/output-style/language 直达设置页；预算 80% 预警告（BudgetWarning 事件）

### P2 增强（用户确认全做）
- **Skill 四字段**：autoUse（off/suggest/prefer/require 路由 + 索引标注）/readOnly（子代理只读约束）/model（专用模型覆盖）/requires（依赖声明）；SKILL.md frontmatter 互通
- **/skills 开关与预览**：SkillStore disabledNames 持久化 + setEnabled（内置不可禁用）；设置页技能开关 + 内容预览对话框
- **设置页命令直达**：/skills /memory /mcp 等命令自动滚动定位对应区块（scrollToItem）
- **余额显示**：loadBalanceText 抽到 AppViewModel 复用；会话成本条回合后显示余额

### 其他
- 单元测试 128 → 134（PlanGate 跨会话隔离/submitPlan phase、SkillParser 新字段、SkillStore 禁用、码点截断）

---

## 2026-08-10（三批 · Cloud 版能力差距全量修复 · 128 测试全绿）

依据《Android端与Cloud版Agent能力差距分析20260810.md》14 项差距一次性全量修复：

### 关键 Bug 修复
- **删除会话复活**：SessionCascade 级联清理（各工程 .reasonix-backup/{id}.json + 检查点 + 删除黑名单防恢复）；删除工程级联清理下属会话与 App 元数据（不碰用户磁盘文件）；删除前确认弹窗
- **成本口径 13 倍差距**：会话详情 ContextBar 恒显会话累计（与成本页同源），最近一步仅执行中附显；reloadSession 清 lastCost
- **任务"时有时无"**：prune(10)→pruneByAge(30 天) 首次进页清理 + take(200)；工程折叠/展开记忆
- **斜杠命令"没反应"**：点击回填输入框（设置类）+ 全部命令具体结果 toast；jobs 命令可用
- **停止按钮滞后**：OkHttp readTimeout 300s（原 0 无限等待）+ aborting 即时反馈（三处按钮变灰"正在停止…"）
- **锁/目标图标**：🎯 emoji→Material Place 图标；切换 Snackbar 反馈；首次使用引导；目标对话框帮助文案

### UI 增强
- **执行状态三处动效**：底部常驻执行指示条（spinner+动作文案轮播+计时）/ 中间文字"等待执行结果"提示 / todo 当前项脉冲高亮；工具链 ⏳→旋转动画
- **页签标题**：五页 PageHeader 页头（会话/文件/任务/统计/设置）+ 底部导航图标文字标签
- **成本模型分组**：统计页模型分区（Pro/Flash 三档可展开）、会话卡分区分行、缓存节省金额、价格表+口径说明折叠区

### 架构级改造
- **执行轨迹步骤树**：TurnRecord/StepRecord 持久化（状态/耗时/产物引用），旧会话回填兼容；完成后消息流可展开复盘（TurnTreeCard），失败红标
- **Skill 生态（对齐 PC 版）**：SKILL.md 格式解析互通；内置 9 技能（init/explore/research/review/test + frontend-report-html/data-analysis/doc-outline/ppt-html，离线可用）；URL 链接一键安装；run_skill/read_skill/install_skill 三工具；技能索引注入（4KB 截断）+ 触发词匹配；点名缺失技能明确询问不静默回退
- **计划模式状态机**：PlanGate（规划→审批→执行）；submit_plan 提交通道；PlanReviewSheet 审批层（批准/驳回带意见/拒绝）；写工具代码级拦截（PLANNING 阶段强制拒绝，批准后自动放行）

### 其他
- 单元测试 96 → 128（新增 CostStats/PlanGate/TurnTracker/ContextBarInfo/ToolRegistry/SkillEngine 及既有套件扩充）

---

## 2026-08-09（二批 · 真机反馈驱动 · 96 测试全绿）

### 界面与交互
- **底部导航栏**：图标对齐修正（等宽均分，任意屏宽图标与选中滑块精确重合）；去掉灰色点按/过渡动效（仅保留紫色滑块滑动）；整栏抬高空出系统导航条
- **文件页签**：单击文件夹进入/单击文件打开全屏编辑预览（修复单击无响应的手势冲突）；搜索结果点目录直接进入；顶部工程名可点击下拉切换工程（删除工程自动联动消失）
- **输入行重排**：输入框紧凑单行高度、文字垂直居中、回车换行保留；⬍ 最大化移入输入框内；发送改为蓝色圆形向上箭头；/ 与 + 缩小
- **review 审批按钮**："仅本次允许" → "仅本次"（防换行变两倍高）
- **会话页**：工程目录默认折叠（点击展开/收起）；工程行右侧 + 直接在该工程下新建会话；新建工程输入的名字必定生效

### 返回键（新增）
内部页面返回上一层（搜索→清搜索、子目录→上级、会话→列表）；根部页面弹"退出 Reasonix？"确认后才退出；对话框/弹层返回正常关闭

### 任务页（修复 + 重构）
- **修复根因**：任务状态/日志/进度不落盘（保存逻辑重新读盘导致修改全丢）——任务页此前永远显示"运行中"、日志 0 行
- 任务按 工程 → 会话 树状展示；已完成任务保留且最新在前；清空日志按钮
- 工程名随任务快照保存（不依赖注册表解析，显示正确）

### 会话任务清单（todo）
Agent 添加任务后实时上屏（原要等很久才显示，像卡住）；无任务时显示"规划中…"；TodoSheet 打开期间实时刷新

### 完整环境
- **启动自动解压**：首次打开自动解压，首启向导页显示进度，完成后提示；不再要求手动去设置页
- 修复：解压中途杀进程产生半成品会自清理重试

### 文件工作区（关键修复）
- **修复**：选已有文件的文件夹做工程，Agent 能看到全部文件（含子文件夹）并可读写编辑
  - 未授权"所有文件访问"时自动经 SAF 直连所选目录（无需额外操作）
  - 授权"所有文件访问"后直接读写真实目录（更强）
- **启动时弹一次授权引导**（Android 11+ 未授权且未点过"暂不"）：[去授权] 跳系统授权页（一次永久生效）/ [暂不] 不再打扰

### 其他
- 事实记忆 id 碰撞修复（同毫秒添加可能误删）
- 单元测试 85 → 96（新增任务存储 4 项：兼容/往返/落盘回归/清理保最新）

## 2026-08-09（一批 · 全面修复版）
工程注册表架构（目录即工作区 2.0）、SAF 真实目录统一、5 个交互 Bug（会话锁/进程击杀/熔断/排队/孤儿兜底）、回合执行总览卡、全屏预览+编辑（文本/图片/Office）、Termux 环境解压修复（PAX 对齐）、5 项 UI 修复（欢迎页暗色/导航栏/会话页隐藏导航栏/多行输入/文件目录导航）

---
构建：`./gradlew.bat :app:assembleDebug`（JAVA_HOME=C:/Program Files/Java/jdk21/jdk-21.0.6+7）；APK 输出 `app/build/outputs/apk/debug/app-debug.apk`
