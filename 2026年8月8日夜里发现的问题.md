# Reasonix SAF 文件系统改造方案

> 作者：Reasonix 开发者 + AI Agent 协作
> 日期：2026-08-09
> 背景：实测发现 Reasonix 文件操作存在"三套隔离世界"问题，本文档给出根因分析与改造方案。

---

## 一、问题根因（实测证据）

本次会话实测暴露 4 个核心事实：

| # | 现象 | 根因 |
|---|------|------|
| 1 | 文件工具操作的是 `/data/data/com.reasonix.mobile/files/projects/p_cc9e6186/`（app 私有目录），而 SAF 真实目录是 `/storage/emulated/0/Download/MLXproject/`，**inode 不同** | 文件工具（list_files/write_file 等）的"项目根"被映射到了 app 私有拷贝，不是 SAF tree URI 指向的真实目录 |
| 2 | 删除 `项目治理规划.md` 后，用户看到"真实文件夹里还有" | 私有目录删了，但 SAF 目录没同步（或从未做同步） |
| 3 | `shell` 和 `python_exec` 的工作目录在 Termux 沙盒 HOME（`.../termux/.../files/home`），与项目目录完全隔离 | 两个环境之间没有桥接 |
| 4 | Termux 内 `python3` 能跑但缺 `threading.py`、`apt` 缺 `libzstd.so.1`、busybox 无 `zip` | 打包的 Termux 发行不完整（最小集合裁剪过度），且 Python 的 prefix 与安装路径不匹配 |

**结论：当前架构是"三套隔离的世界"——SAF 真实目录 / app 私有项目拷贝 / Termux 沙盒。**

---

## 二、可选方案

### 方案 A：统一工作区 = SAF 真实目录（推荐）

让"项目工作区"直接就是 `content://` tree URI 指向的目录，所有工具（文件工具 + shell + python）都基于它操作。

**实现思路：**
- 项目创建时，把 SAF tree URI 通过 `DocumentsContract.buildDocumentUriUsingTree()` 解析出真实路径（Android 11+ 可用 `DocumentsContract.getDocumentId()` + 自己解析 `primary:` 前缀 → `/storage/emulated/0/...`；或用 `ExternalStorageProvider` 反查）
- 让 `list_files/read_file/write_file/edit_file` 的根目录绑定到这个真实路径，而不是 app 私有目录
- `shell` 的 cwd 也设置为该路径；`python_exec` 同理
- **关键点**：如果 app 已申请 `MANAGE_EXTERNAL_STORAGE`（所有文件访问权限），Termux 子进程就可以直接读写 `/storage/emulated/0/...`，无需 SAF API，路径级访问最省事、性能最好

**优点：** 一处改动解决所有隔离问题；用户看到的 = Agent 操作的；无同步需求；性能好。
**缺点：** 需要 `MANAGE_EXTERNAL_STORAGE`（Google Play 上架审查严格，但侧载/企业场景没问题）；Android 11+ 路径访问受 scoped storage 限制，非媒体文件必须靠该权限。

### 方案 B：桥接代理（不依赖 MANAGE_EXTERNAL_STORAGE）

App 进程持有 SAF 权限，把文件操作做成 **ContentProvider 或本地 RPC** 暴露给 Termux。

**实现思路：**
- App 内实现一个 `FileBridgeService`（ContentProvider 或 unix socket），接收"read/write/list/delete/move"请求，内部用 `DocumentFile`/`DocumentsContract` 操作 SAF 真实文件
- Termux 里放一个薄客户端（如 `fsx` 命令），把文件工具和 shell 命令的输出转发过去
- 项目根 = tree URI，工具层把相对路径拼到 tree URI 上

**优点：** 完全合规（只用 SAF），无需 MANAGE_EXTERNAL_STORAGE；通用性强（shell 里任何命令都能配合）。
**缺点：** 实现量较大；DocumentFile 没有高效的批量/rename，性能一般；大文件要走流式。

### 方案 C：镜像 + 双向同步（不推荐）

保留私有工作区，另起同步线程把变更推到 SAF 目录。

**优点：** 改动最小。
**缺点：** 正是本次"删了还在" bug 的来源；冲突处理复杂；用户观感极差。**强烈不建议。**

---

## 三、具体建议

1. **首选方案 A**，理由：
   - Termux 沙盒 shell 实测**能读** `/storage/emulated/0/Download/MLXproject/`（此前 ls 成功），说明沙盒进程已有存储访问能力，只是工作区没指过去
   - 一旦把项目根指到真实路径，文件工具和 shell 天然统一，零同步、零欺骗

2. **配套修复 Termux 发行打包**：
   - `threading.py` 缺失 = Python 标准库没打全。建议用 Termux 官方 `python` 包的完整结构打包，或安装时用 `pkg install python` 由 apt 补齐（apt 缺 `libzstd.so.1`，说明动态库也不全）
   - 检查 `usr/lib/python3.14/lib-dynload/` 是否完整，`PYTHONHOME` 是否与安装前缀一致
   - 至少保留 `zip/unzip`（或 busybox 全功能版），文件操作太常用

3. **过渡期小改进**：如果短期改不动方案 A，至少把 `shell` 和 `python_exec` 的默认 cwd 设置为项目真实目录（哪怕先只读），并给文件工具加一条"真实路径映射"的配置项，让 Agent 知道该往哪写。

---

## 四、后续 TODO

- [x] 验证 Termux 子进程对 `/storage/emulated/0` 的写权限（用 `touch` 测试）✅ 已实测通过（可写）
- [ ] 实现 SAF tree URI → 真实路径解析模块
- [ ] 评估 MANAGE_EXTERNAL_STORAGE 上架影响
- [ ] 修复 Termux 发行打包（python 标准库、动态库、zip）
- [ ] 为文件工具添加"真实路径映射"配置项

---

## 五、开发者补充需求（PC 端一并修改）

> 以下为开发者 2026-08-09 补充的原始需求，交给 PC 端一并处理。

### 5.1 核心问题确认

1. **文件功能操作的是镜像文件，不是真实磁盘系统文件**：实测确认，文件工具操作的是 app 私有目录的镜像拷贝，而非 SAF 授权的真实磁盘目录。用户看到的"删除后文件还在"正是由此产生。
2. **功能上缺失基本交互逻辑**：当前文件管理功能缺少常用文件管理器的基础交互，例如：
   - 单击打开文件夹
   - 退出文件夹（返回上级）
   - 修改/重命名
   - 移动、复制、粘贴
   - 等常用文件管理器功能

### 5.2 设计初衷

当初设计此功能时，定位就是**代替手机端的文件管理器**——但它是"受限版"文件管理器，**只可以查看或操作用户选中的项目对应的真实目录**，而非整个存储空间。

### 5.3 需要追加的功能：项目切换

- 当用户在绘画（对话/创建）界面创建**多个项目**时，自然对应**不同的文件夹**。
- 因此文件管理工具必须支持**切换项目**，即在不同项目的真实目录之间切换查看与操作。

### 5.4 最终结论（硬性要求）

> **所有 Agent 操作文件、执行脚本，最终结果必须能够直接操作系统的原文件目录，而不是镜像目录。**

- 这是本次改造的**唯一验收标准**。
- 至于"因为是安卓版、装的是沙盒还是 tmux"，属于**纯技术实现问题**，开发者不关注具体技术形态，只关注最终结果：Agent 的操作要落到真实磁盘文件上。
